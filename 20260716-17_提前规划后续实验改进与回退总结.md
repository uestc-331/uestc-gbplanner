# 2026-07-16 至 07-17 提前规划后续实验改进与回退总结

## 本次回退目标

当前工作区以“安全提前规划已经加入，但尚未加入最新地图复检”的版本为基础。
目标快照为：

```text
config_backups/rmf_obelix_before_adaptive_preplan_recheck_20260716/
```

该快照保留后台提前规划、执行完上一段后再切换、缓存路径起点匹配和缓存超时，
但不包含自适应提前量和提交前最新地图复检。

回退前的完整实验版本保存在：

```text
config_backups/rmf_obelix_before_rollback_to_preplan_no_recheck_20260717/
```

## 2026-07-17 回退执行结果

- 已从目标快照恢复 PCI 提前规划核心代码和配置。
- 已移除最新地图复检服务 `planner_validate_path.srv` 及其构建依赖。
- 已撤销自适应提前量、固定全局边界盒、快速复检、异步复检、局部路径优化器和根边诊断。
- 已保留提前规划所需的执行进度、序列号防过期、缓存超时和完成后切换逻辑。
- 配置恢复为固定提前量：`preplan_trigger_remaining_time: 4.0`，缓存有效期为 `8.0` 秒。
- 两个 YAML 配置均通过 `yaml.safe_load` 解析。
- 执行 `catkin build planner_common gbplanner planner_control_interface pci_general --no-status`，27 个相关包全部编译成功，`Failed: None`。
- 编译警告来自仓库原有的未使用变量、初始化顺序和日志格式问题，本次回退没有产生编译错误。

## 2026-07-17 回退后重新加入的轻量路径优化

在不恢复缓存路径提交前复检的前提下，重新加入了一个受限的局部路径后处理器：

1. RRG 先生成并筛选可行的局部探索路径。
2. 使用碰撞检查通过的捷径连接删除冗余折点。
3. 使用 ESDF 梯度、平滑力和原路径锚定力小范围移动中间路径点。
4. 只有平滑度或净空代价确实改善、净空没有明显退化且最终碰撞检查通过时才采用优化路径。
5. 优化超时、无有效移动、没有改善或最终检查失败时，立即使用优化前路径，不因优化失败重新运行 RRG。

默认限制如下：

```yaml
final_shortcut_enable: false
final_shortcut_max_skip: 6
path_optimizer_enable: true
path_optimizer_iterations: 8
path_optimizer_max_time: 0.08
path_optimizer_max_deviation: 0.25
path_optimizer_min_clearance: 0.55
path_optimizer_fix_start_points: 2
```

该功能只接入常规局部探索路径。全局路径在进入局部路径输出前已经独立返回，
时间/电量返航路径也在优化代码前返回，因此不处理全局规划、返航和手动路径。

RViz 中 `/vis/optimized_path` 使用青色显示实际被接受的优化路径；如果优化器回退，
该标记会被清除。日志使用 `[RRG][SHORTCUT]` 和 `[RRG][OPT]`。

修改前备份：

```text
config_backups/rmf_obelix_before_fast_fallback_path_optimizer_20260717/
```

验证命令 `catkin build planner_common gbplanner --no-status` 通过，24 个相关包全部成功，
`Failed: None`。

### 运行日志复盘后的优化器修正

实际运行没有出现 `fallback=final_safety`，也没有出现优化路径最终碰撞检查失败。
多数未采用优化路径的原因是 `fallback=no_improvement`：旧力模型始终施加归一化 ESDF
推力，在净空已经足够的开阔区域也会扰动路径。例如一次日志中净空从 `1.646m`
提高到 `1.782m`，但平滑度代价从 `0.0067` 恶化到 `0.0563`。

同时，捷径化曾多次将路径从 9 或 11 个 RRG 顶点缩减为 3 个顶点，切弯幅度过大，
不适合当前存在点云误差的场景。因此做了以下修正：

- 关闭 `final_shortcut_enable`，保留 RRG 路径拓扑；
- 仅当路径点净空低于 `centerline_clearance_target=1.20m` 时启用 ESDF 离墙推力；
- 开阔区域只施加平滑力和原路径锚定力；
- 每次候选移动要求 ESDF 净空不下降；
- 净空改善只有在平滑度不明显恶化时才会被接受；
- 优化预算调整为 8 轮、`0.08s`。

最终墙体、天花板和地板安全检查继续保留。原因是当前地图没有语义信息可以把真实结构面
和误识别 occupied 体素完全区分，直接关闭检查不能保证不撞墙；同时日志已证明该检查不是
本轮优化回退的来源。

本轮修正前备份：

```text
config_backups/rmf_obelix_before_clearance_gated_optimizer_20260717/
```

## 2026-07-17 blocked-root recovery 与全局高度修复

一次实际卡住中，局部 RRG 连续得到 `1 vertex / 0 edges`，但最小起点盒检查为
`free`。根节点附近的 `/surface_pointcloud` 中，最近两个表面点距离分别约为
`0.446m` 和 `0.478m`：它们位于静态起点盒外，却会被向外移动的碰撞盒扫到，
导致约 700 次根边连接全部失败。旧 `start_recovery` 只在起点 occupied 时运行，
因此无法处理“起点 free、所有出口 blocked”的情况。

新增 blocked-root recovery：

- 仅在 `buildGraph()` 返回 `ERR_NO_FEASIBLE_PATH` 且最小起点盒为 free 时触发；
- 复用周向恢复候选采样，但目标点必须使 ESDF 净空增加至少 `0.15m`，或达到
  `0.75m`；
- 近端最多 `0.8m` 使用最小碰撞盒；
- 近端 occupied 仅在 ESDF 净空不低于根节点、且沿路径不明显下降时暂时容忍；
- 超过 `0.8m` 后恢复当前规划碰撞盒和严格 free 检查；
- 成功后输出一段 `0.8-2.2m` 的恢复路径，到达更干净区域后再恢复正常规划。

同一次运行中，局部失败后全局规划虽然选到了 frontier，但路径首段从
`z=2.801m` 到 `z=2.601m`，被缩减后的全局上边界拒绝。根因是两个全局入口都对
无人机错误执行了地面机器人高度投影：

```cpp
cur_state.z -= (robot_height - max_ground_height);
```

当前参数使该表达式把无人机高度抬高约 `0.20m`。现已限制为仅非空中机器人执行，
空中机器人使用真实当前高度连接全局图。全局边界检查本身继续保留。

修改前备份：

```text
config_backups/rmf_obelix_before_blocked_root_and_aerial_global_height_fix_20260717/
```

验证命令 `catkin build gbplanner --no-status` 通过，24 个相关包全部成功，
`Failed: None`。

## 回退后仍保留的功能

- 执行当前局部路径期间，后台规划下一条局部路径。
- 下一条路径只会缓存，不会覆盖正在执行的路径。
- 当前路径执行完成并进入 READY 后，才提交缓存路径。
- 使用执行序列号丢弃过期的后台规划结果。
- 缓存路径具有有效期和起点误差检查。
- 全局规划、返航、手动路径不会启动局部提前规划。
- RViz 保留提前规划状态文字。

## 后续做过但本次撤销的改进

### 1. 自适应提前规划时机

根据最近规划延迟的 90% 分位数加安全余量，动态决定提前多久开始规划，
并限制在最小和最大提前时间之间。

效果：能够适应规划时间波动，比固定 4 秒更容易在当前路径结束前得到下一条路径。

撤销原因：它与后续复检、重规划流程组合后，整体状态机复杂度明显增加。

对应备份：

```text
config_backups/rmf_obelix_before_adaptive_preplan_recheck_20260716/
```

### 2. 缓存路径提交前最新地图复检

增加 `planner_validate_path.srv`，在缓存路径真正执行前，使用最新地图再次检查
碰撞、unknown、全局边界和 clearance。

后续又依次尝试：

- 固定独立 Global 边界安全盒；
- 按当前位置裁剪缓存路径；
- 只做碰撞复检，跳过重复 clearance；
- 使用最小安全盒复检；
- 近机体 0.8 米使用真实机体盒；
- 将复检放到独立 ROS callback queue。

效果：复检耗时从约 4 至 6 秒降低到约 0.003 至 0.016 秒。

问题：地图中的伪 occupied/unknown 仍会拒绝视觉上安全的路径；复检失败后会重新进入
完整规划，使状态机和日志分析变得复杂。

对应备份：

```text
config_backups/rmf_obelix_before_fixed_global_bound_box_20260716/
config_backups/rmf_obelix_before_fast_collision_only_recheck_20260716/
config_backups/rmf_obelix_before_async_near_robot_recheck_20260717/
```

### 3. 固定全局边界碰撞盒

将 Global 边界收缩尺寸与动态规划碰撞盒解耦，避免路径采样在边界内，
但最终检查因碰撞盒尺寸变化被判越界。

效果：解决了一部分“规划时合法、复检时超出限高”的不一致。

### 4. 局部快速路径优化器

在 RRG 选出局部路径后，使用有限次迭代执行：

- 相邻点平滑；
- ESDF 梯度推动路径远离障碍；
- 原路径锚定和最大偏移限制；
- 每次移动后的相邻线段碰撞检查；
- 最终整条路径安全检查；
- 优化失败自动回退原始路径。

优化器只用于局部探索路径，不用于全局规划、手动返航、时间返航和距离返航。

运行中典型结果：

```text
耗时：0.01 至 0.33 秒
最小净空：1.057 m -> 1.411 m
最小净空：1.488 m -> 1.694 m
```

问题：部分路径点最大移动达到 0.60 米，虽然碰撞检查通过，但会增加轨迹跟踪难度；
它不能解决 RRG 根本连不出第一条边的情况。

对应备份：

```text
config_backups/rmf_obelix_before_local_path_optimizer_20260717/
```

### 5. 根节点首边放宽与诊断

首边前 0.8 米使用 `size + size_extension_min` 最小安全盒，后续恢复完整规划盒，
unknown 仍然拒绝。

又增加了 `/vis/root_edge_diagnostics`：水平 16 个方向，每方向检查
0.2、0.4、0.6、0.8 米，绿色表示 free、红色表示 occupied、黄色表示 unknown。

一次典型卡死诊断结果：

```text
free=26
occupied=38
unknown=0
fully_free_dirs=1/16
```

结论：`1 vertex / 0 edges` 发生在路径评分和轨迹优化之前，主要是局部体素地图将
根节点周围扫掠通道判为 occupied，并不是平滑、前方偏好或信息增益筛选过严。

对应备份：

```text
config_backups/rmf_obelix_before_root_edge_diagnostics_20260717/
```

### 6. PCI 执行卡点保护

增加航点长期无进展检测，超时后释放当前轨迹并允许重新规划。

发现的问题：当前参数形成了死区：

```text
航点到达阈值：0.30 m
stuck_dist_margin：0.25 m
实际稳定距离：约 0.49 m
```

0.49 米既大于到达阈值，又小于卡住触发阈值 0.55 米，导致 PCI 长时间停在同一航点。

建议以后重新启用时使用更小的 margin，并将“无进展时间”作为主要判据。

## 后续重新实施建议

建议一次只恢复一个独立模块并做对照：

1. 先使用当前回退版验证提前规划本体稳定性。
2. 单独修复地图伪 occupied 和自由空间清除。
3. 再加入轻量路径优化，最大偏移建议先限制为 0.20 至 0.30 米。
4. 最新地图复检最后加入，并只做快速碰撞检查。
5. PCI 卡点保护应消除阈值死区后再启用。

不要同时恢复全部实验功能，否则难以判断卡住来自规划、复检、优化还是执行器。
