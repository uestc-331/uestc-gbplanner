# 20260709-17 GBPlanner 算法改进、实验复盘与简历总结

## 1. 文档定位

本文总结 2026 年 7 月 9 日至 7 月 17 日围绕 GBPlanner3 开展的仿真规划改造、性能优化和失败实验，基于当前分支：

```text
gbplanner3real2sim
```

对比基线为原有 `gbplanner3-real / GBPlanner3` 的局部 RRG 探索、全局图规划、Voxblox 建图和 PCI 轨迹执行框架。

本文将功能分为三种状态：

- **当前保留**：已存在于当前工作区，完成编译或运行验证。
- **实验后回退**：曾完成实现和测试，但因复杂度、误判或收益不足而撤销。
- **后续方向**：经过日志定位后形成的下一阶段方案，尚未作为当前成果宣称。

## 2. 项目摘要

针对 GBPlanner3 在复杂点云环境和长航程自主探索中出现的贴墙飞行、起点伪占据、RRG 根节点失连、规划停顿、局部路径弯绕以及规划与执行串行等待等问题，构建了一套面向无人机自主探索的安全增强与低停顿规划框架。

核心工作包括：

1. 基于 ESDF 的多目标安全路径评价，使路径选择从“仅可通行”提升为“兼顾净空、居中性、方向连续性和平滑性”。
2. 设计分层起点状态判定与 blocked-root 恢复机制，解决起点盒可用但 RRG 无法生成第一条边的问题。
3. 设计异步提前规划和完成后原子切换机制，在当前轨迹执行期间计算下一段路径，降低规划引起的悬停等待。
4. 构建受限 ESDF 局部路径优化器，在 RRG 找到可行路径后进行快速平滑和离障优化，失败时无条件回退安全原路径。
5. 增加基于历史探索方向的前方优先策略，延迟无必要的侧后方分支和全局重定位。
6. 修复空中机器人全局路径高度投影错误，隔离局部优化、全局规划、手动返航、时间返航和里程返航的控制边界。
7. 打通 Gazebo Livox 点云到 Voxblox 的数据链路，并加入模型感知的无人机自点云过滤，降低机身和旋翼反射造成的伪占据。
8. 建立 RRG 分阶段诊断、RViz 状态文字、候选路径排名和分段耗时观测，提高规划失败的可解释性。

## 3. 原版 GBPlanner3 的基本能力

原版系统已经具备以下主干能力：

- 使用 Gazebo 或真实传感器点云更新 Voxblox TSDF/ESDF 地图。
- 在局部空间通过 RRG 随机采样、碰撞检查和连边构建探索图。
- 根据未知空间收益和路径代价选择局部探索路径。
- 局部探索耗尽后，将已探索状态维护到全局图并执行全局重定位。
- 通过 Planner Control Interface 将规划路径转换为轨迹并交给控制器执行。
- 支持全局规划、返航和基础 RViz 可视化。

本次工作没有替换上述主体框架，而是在地图输入、候选路径评价、根节点恢复、路径后处理和规划执行并行化等关键环节增加工程化能力。

### 3.1 原版与当前版本对照

| 能力维度 | 原版 GBPlanner3 | 当前改进版本 |
|---|---|---|
| 点云输入 | 传感器点云直接进入 Voxblox | TF/时间戳修正，机身与旋翼模型感知自滤波 |
| 碰撞安全 | 以占据查询和碰撞盒可行为主 | 保留碰撞硬约束，增加 ESDF 最小净空与最终复核 |
| 路径居中 | 无明确管道中心偏置 | 以平均/最小 ESDF 净空构造中心线偏置 |
| 探索方向 | 每轮主要按局部收益选择 | 引入历史探索方向、方向衰减和前方完成门控 |
| 起点判定 | 受动态 bound mode 和碰撞盒影响 | 固定最小物理起点盒，与规划边界盒解耦 |
| 根节点恢复 | 主要处理起点 occupied | 增加 dirty-start、dirty-root 和 blocked-root 分层恢复 |
| 路径平滑 | 主要依赖 RRG 路径及原有后处理 | 增加限时、限偏移、可回退的 ESDF 局部优化器 |
| 规划与执行 | 当前轨迹完成后再规划 | 执行期后台规划，完成后原子提交缓存路径 |
| 全局高度 | 复用地面机器人高度投影逻辑 | 空中机器人使用真实高度连接全局图 |
| 故障诊断 | 通用日志和路径 Marker | 候选排名、阶段通过率、根边原因、RViz 文字和分段计时 |
| 返航隔离 | 原有全局/返航流程 | 明确禁止局部优化和提前规划侵入全局、手动及预算返航 |

## 4. 当前算法总流程

```text
Gazebo Livox 点云
  -> 坐标与时间戳校正
  -> 机身/旋翼自点云过滤
  -> Voxblox TSDF/ESDF 更新
  -> 固定最小起点盒检查
  -> 局部 RRG 采样、碰撞检查和连边
  -> 探索收益、历史方向、净空、居中和平滑综合评价
  -> 最终安全检查
  -> 受限 ESDF 快速路径优化
  -> PCI 缓存或立即提交
  -> 当前轨迹执行期间后台提前规划
  -> 当前轨迹完成后原子切换缓存路径
  -> 局部耗尽后进入全局重定位或返航状态机
```

异常恢复链路：

```text
起点最小盒 occupied
  -> START_RECOVERY

起点最小盒 free，但 RRG 为 1 vertex / 0 edges
  -> BLOCKED_ROOT recovery

局部探索确认耗尽
  -> Global graph reposition

时间或里程预算到达
  -> Homing，绕开局部优化与提前规划
```

## 5. 相比原版 GBPlanner3 的主要改进

### 5.1 ESDF 净空约束与管道中心偏置

**状态：当前保留**

#### 原始问题

原版候选路径主要围绕探索收益和路径长度排序。路径只要碰撞盒查询为 free，就可能沿墙、天花板或地板附近通过。在管道或走廊场景中，这会放大点云误差、控制跟踪误差和机体尺寸误差带来的碰撞风险。

#### 改进方法

在 RRG 候选路径评价中加入基于 ESDF 的分层安全目标：

- `wall_clearance_min`：最终路径最低净空硬约束。
- `wall_clearance_soft`：净空不足时的连续软惩罚。
- `centerline_clearance_target`：推动路径向更大平均净空区域靠拢。
- 沿路径按固定步长采样 ESDF，而不是只检查端点。
- 最终路径再次执行净空硬检查，避免后处理破坏安全性。

当前核心参数：

```yaml
wall_clearance_min: 0.55
wall_clearance_soft: 0.95
wall_clearance_penalty: 1.5
centerline_clearance_target: 1.20
centerline_penalty: 1.0
```

#### 相比原版的提升

路径评价从二值碰撞可行性扩展为带安全裕度的多目标优化：

```text
探索收益
- 路径长度代价
- 靠墙净空代价
- 偏离中心区域代价
- 转折平滑代价
- 历史方向偏离代价
```

该设计保持碰撞安全为最高优先级，同时在多条可行路径中优先选择更居中、更稳定的路径。

### 5.2 历史探索方向驱动的前方优先策略

**状态：当前保留**

#### 原始问题

局部 RRG 每轮独立根据收益选路，容易在侧向或后方小分支之间切换。无人机可能尚未完成主方向探索，就频繁触发全局重定位或回头探索。

#### 改进方法

- 使用历史已执行探索方向作为参考方向，而不是瞬时机头 yaw 或固定世界坐标轴。
- 对候选路径计算方向点积并施加连续方向代价。
- 维护方向历史和衰减，降低瞬时路径抖动对参考方向的影响。
- 只有当前向前方向缺少足够有效候选时，才允许局部完成判定和全局分支切换。

#### 相比原版的提升

将局部规划从“逐轮贪心探索”升级为具有短期任务记忆的方向一致性探索，使主通道推进和支路回访形成明确优先级。

### 5.3 稳定最小起点盒与分层起点恢复

**状态：当前保留**

#### 原始问题

原版起点检查会受到 `bound=0/1/2` 和动态碰撞盒尺寸影响。同一位置可能一轮被大盒判断为 occupied，下一轮又被小盒判断为 free，导致错误进入恢复流程并连续返回 empty path。

#### 改进方法

1. 将“起点是否干净”与当前规划边界盒解耦。
2. 固定使用：

```text
RobotParams.size + RobotParams.size_extension_min
约等于 [0.60, 0.60, 0.55]
```

3. 最小盒为 free 时跳过普通 `START_RECOVERY`。
4. 最小盒确实 occupied 时，才执行周向候选逃逸。
5. 候选逃逸方向优先满足 ESDF 净空增加，避免恢复路径朝墙移动。
6. Gate search 临时修改碰撞盒后恢复原 bound mode，防止状态污染后续规划。

#### 相比原版的提升

将起点状态从依赖当前规划模式的瞬时判断，改造成具有固定物理语义的稳定判定，显著降低“起点明明可用却误触发 recovery”的状态振荡。

### 5.4 Blocked-root recovery

**状态：当前保留**

#### 原始问题

实际日志多次出现：

```text
START_CHECK_BOX = free
Graph = 1 vertex / 0 edges
```

这不代表采样器没有运行，而是根节点存在，但所有第一条候选边都在扫掠碰撞检查中失败。静态起点盒可以是 free，但移动碰撞盒会扫到起点周边伪 occupied 体素，因此普通 start recovery 不会触发，RRG 又无法离开根节点。

#### 改进方法

- 在 `buildGraph()` 返回无可行路径且最小起点盒为 free 时，识别 blocked-root 状态。
- 复用周向恢复采样，但要求目标净空至少提升 `0.15 m` 或达到 `0.75 m`。
- 近端采用最小碰撞盒，并仅对净空不下降的短 occupied 前缀提供受限容忍。
- 离开近端区域后恢复正常碰撞盒和严格 free 检查。
- 输出短恢复路径后，重新进入正常 RRG 探索。

#### 相比原版的提升

增加了“静态可站立但动态不可离开”的拓扑失连识别，覆盖了普通 occupied-start recovery 无法处理的中间状态。

### 5.5 Dirty-start edge 与 dirty-root escape

**状态：当前保留，但属于受限容错机制**

#### 原始问题

传感器自身点云、时间同步误差和 TSDF 历史权重会使根节点附近残留少量伪占据。一旦第一段边被严格否决，后续所有远处安全候选都无法加入图。

#### 改进方法

- 仅在根节点附近允许裁剪被污染的路径前缀。
- 要求裁剪后路径严格通过碰撞检查和全局边界检查。
- 要求运动方向朝更高 ESDF 净空区域发展。
- 不放宽路径中后段，也不取消墙体、地板和天花板安全约束。

#### 工程意义

这不是关闭碰撞检查，而是针对地图局部污染设计的单调净空逃逸约束，使系统在感知误差存在时仍有有限自恢复能力。

### 5.6 执行期异步提前规划与完成后切换

**状态：当前保留**

#### 原始问题

原版执行链路基本是：

```text
执行当前路径 -> 停下 -> 请求下一次规划 -> 等待 -> 执行下一条路径
```

即使单次规划只需数百毫秒，连续 empty path 重试或复杂地图规划仍会形成明显悬停。

#### 改进方法

- 当前轨迹剩余时间小于阈值且仍有足够剩余距离时，在后台线程请求下一条局部路径。
- 后台结果只写入缓存，不立即覆盖正在执行的轨迹。
- 当前路径完成并进入 READY 后，才原子提交缓存路径。
- 使用执行序列号丢弃迟到结果，防止旧规划覆盖新任务。
- 缓存设置最大年龄、最小路径点数和起点容差。
- 全局规划、手动路径和返航状态不触发局部提前规划。

当前参数：

```yaml
preplan_trigger_remaining_time: 4.0
preplan_min_current_remaining_distance: 0.50
preplan_retry_interval: 1.5
preplan_cache_max_age: 8.0
preplan_start_tolerance: 0.60
```

#### 相比原版的提升

将规划与执行由串行流水线改造成受状态机约束的异步双缓冲架构，在不抢占当前轨迹的前提下隐藏下一轮局部规划延迟。

### 5.7 RRG 后置快速局部路径优化器

**状态：当前保留**

#### 原始问题

通过不断增加 RRG 候选筛选条件来寻找“天然平滑且居中”的路径，会降低随机图有效采样率，并增加连边和碰撞检查负担。

#### 改进方法

采用“先找可行路径，再做受限优化”的两阶段架构：

1. RRG 先完成可行路径生成和基础安全筛选。
2. 对中间路径点施加平滑力、原路径锚定力和按需 ESDF 净空力。
3. 只有净空低于 `1.20 m` 时才启用离障推力，避免开阔区域被无意义扰动。
4. 每个点最大偏移限制为 `0.25 m`，起点附近两个点固定。
5. 优化最多 8 轮、预算 `0.08 s`。
6. 优化结果只有在质量改善且最终安全检查通过时才采用。
7. 超时、无改进或安全失败时立即回退 RRG 原始路径，不重新运行 RRG。

运行实验曾观察到：

```text
最小净空 1.057 m -> 1.411 m
最小净空 1.488 m -> 1.694 m
```

#### 相比原版的提升

把概率采样的拓扑搜索与连续空间的局部几何优化解耦，减少通过堆叠 RRG 硬筛选条件获得路径质量的低效做法。

### 5.8 空中机器人全局高度一致性修复

**状态：当前保留**

#### 原始问题

局部规划失败转入全局图后，空中机器人仍执行了面向地面机器人的高度投影：

```cpp
cur_state.z -= (robot_height - max_ground_height);
```

这会把无人机全局路径连接点错误抬高，造成“采样时在限高内，最终路径却被全局限高拒绝”。

#### 改进方法

- 高度投影仅对非空中机器人执行。
- 无人机使用真实当前位置高度连接全局图。
- 保留全局高度硬边界检查，不通过放宽限高掩盖坐标语义错误。

#### 相比原版的提升

修复了跨机器人类型复用代码造成的状态空间不一致，使全局采样、图连接和最终边界检查使用统一的空中机器人高度语义。

### 5.9 Gazebo 点云链路修复与模型感知自滤波

**状态：当前保留，需重启仿真后生效**

#### 原始问题

Gazebo 中 Livox MID-360 会扫描到无人机自身的机身盒和四个旋翼碰撞圆盘。原始点云实测：

```text
单帧总点数约 22315
0.3 m 内点数约 6071
0.3 至 0.6 m 仍有约 693 个近距离点
```

仅设置 `min_ray_length_m: 0.3` 无法删除 0.3 m 外的旋翼反射，而直接将统一盲区扩大到 0.6 m 又会隐藏真实近距离墙体。

#### 改进方法

- 明确点云链路：`/livox/lidar -> /livox/lidar_fixed -> Voxblox`。
- 在坐标转换后、Voxblox 建图前执行自滤波。
- 按实际 `iris_csj` 模型过滤中央机身盒和四个旋翼圆柱，而不是统一删除近距离点。
- 保留机体几何范围外的真实障碍物。
- 发布 `/livox/lidar_self_points` 调试话题，单独显示被剔除点。
- 日志持续输出输入点数、输出点数和自点云删除数量。

独立合成点云测试结果：

```text
3 个输入点
2 个机身/旋翼点被删除
1 个机外障碍点被保留
```

#### 相比原版的提升

从基于距离的粗粒度盲区过滤升级为基于机器人几何先验的模型感知过滤，降低自反射伪占据，同时保留近场真实障碍感知能力。

### 5.10 可观测性与 RViz 诊断体系

**状态：当前保留**

新增或强化的诊断包括：

- 起点实际检查盒 `/vis/start_check_box`。
- 规划阶段文字 `/vis/planning_diagnostics`。
- 优化路径 `/vis/optimized_path`。
- 提前规划状态和缓存状态。
- RRG 候选路径排名和各筛选项通过情况。
- `START_CHECK`、`BLOCKED_ROOT`、`FORWARD`、`CLEARANCE`、`OPT`、`COMPLETE` 等结构化日志。
- `/gbp_time_log` 分解建图、探索收益、最短路和候选评价耗时。

候选路径日志可直接回答：

```text
有多少候选通过收益阈值？
有多少通过最短长度？
有多少通过方向、前方、净空和中心偏置？
最终是没有边、没有收益，还是被后处理拒绝？
```

这使“无人机卡住”从肉眼现象转化为可定位的算法阶段和拒绝原因。

### 5.11 时间轨迹与离散 waypoint 的前向进度同步

**状态：当前保留，仿真启用，实机默认关闭**

#### 原始问题

PCI 会一次性向轨迹服务器发布完整的时间参数化轨迹，但自身又使用离散 waypoint
距离逐点判断执行进度。如果某次回调没有捕获到无人机进入中间 waypoint 的通过半径，
轨迹服务器仍会继续向后执行，PCI 却会永久等待已经飞过的旧点。一次实际日志表现为：

```text
wp=507/543 dist=0.28
wp=513/543 dist=0.54
wp=513/543 dist=0.48，随后重复 42 次
```

原 stuck 条件还存在 `dist_thr + stuck_dist_margin` 死区：距离既未达到 waypoint
通过阈值，又未达到 stuck 触发阈值，导致状态一直保持 `RUNNING`，已经生成的提前规划
路径无法提交并最终过期。

#### 改进方法

- 在当前索引之后的有界窗口中寻找显著更接近当前位置的 waypoint。
- 索引只允许单调向前，不允许因最近点搜索回退。
- 限制最大搜索窗口、最大匹配距离和最小改善量，避免在回环路径上跨段跳跃。
- 同步到最后一个 waypoint 后，仍使用原有严格终点距离和 yaw 条件完成路径。
- 返航终点阈值不变，实机配置默认不启用前向同步。
- 缩小仿真 stuck margin，消除长时间无法推进也无法释放的距离死区。
- 执行日志增加三轴误差 `dxyz` 和 `[PCI][EXEC][SYNC]` 索引追赶记录。

#### 相比原版的提升

将执行完成判断从“必须逐个命中所有插值采样点”改为“受限地同步到实际沿轨迹进度，
并严格确认最终目标”，解决轨迹真实执行进度与 PCI 状态机进度脱节的问题。

## 6. 性能分析与可量化结果

### 6.1 历史规划耗时样本

一次包含 48 次规划请求的日志统计：

| 指标 | 结果 |
|---|---:|
| 总请求数 | 48 |
| 平均服务耗时 | 0.204 s |
| 中位数 | 0.084 s |
| 最慢 | 1.456 s |
| 成功非空路径平均耗时 | 0.439 s |
| empty path 次数 | 36 |

关键结论：用户感受到的长时间悬停，很多时候不是某一次规划计算特别慢，而是多次 empty path 和状态机重试的累计结果。

### 6.2 最新地图复检实验

完整路径复检初版耗时约 `4 至 6 s`。经过碰撞检查裁剪、最小盒复检和独立 callback queue 后，单次复检可降至约：

```text
0.003 至 0.016 s
```

但该模块最终回退，原因不是计算速度，而是地图伪 occupied 仍会造成错误拒绝，失败后重新进入完整规划反而放大停顿。

### 6.3 局部优化器

- 优化预算固定为 `0.08 s`。
- 优化失败不触发 RRG 重规划。
- 实验中部分路径净空提升约 `0.2 至 0.35 m`。
- 当前关闭激进捷径化，保留 RRG 路径拓扑。

### 6.4 工程规模

当前工作区相对阶段提交 `1dd0a16`，在 GBPlanner ROS 相关模块中新增约 1100 行代码，覆盖 RRG、参数系统、PCI 状态机、RViz 诊断和点云预处理。该数字只用于说明工程范围，不等同于性能收益。

## 7. 失败实验与原因复盘

### 7.1 只靠调参解决所有卡住问题

**现象**：不断调整 `edge_overshoot`、碰撞盒、采样次数和路径长度阈值，效果随场景波动。

**为什么不行**：

- 参数只能改变拒绝概率，无法修复起点盒语义不一致。
- `1 vertex / 0 edges` 可能来自根节点周围伪占据，而不是采样次数不足。
- 点云中存在机体反射时，增加采样只会重复执行更多失败碰撞检查。

**经验**：先判断失败发生在地图输入、根边构建、候选评价还是执行状态机，再决定调参或改算法。

### 7.2 把 `min_path_length` 调得过高或过低

**过高**：大量有效局部脱困路径被 `pass_length=0/N` 拒绝，形成连续 empty path。

**过低**：规划器会接受大量只有 0.2 至 0.6 m 的局部收益路径，无人机表现为短距离来回踱步。

当前还存在 `short_frontier` 特例：即使 `min_path_length=0.8`，高收益密度的短 frontier 路径仍可能被接受。最新日志出现过：

```text
best_len=0.23
short_frontier=1
path_size=3
```

**经验**：最短路径不能只使用单一阈值，后续应加入执行迟滞、最小推进距离和短路径连续次数限制。

### 7.3 过度缩小 `edge_overshoot`

**现象**：碰撞误拒绝减少，但 RRG 更容易保留非常局部的短边和短路径。

**为什么不行**：`edge_overshoot` 是跟踪误差和边端安全余量，不是路径长度控制器。过小会削弱边连接安全裕度，也不能从根本上增加规划视野。

### 7.4 直接放宽或关闭碰撞检查

**现象**：视觉上安全的路径不再被伪点云拒绝，但撞墙、天花板和地板的概率上升。

**为什么不行**：当前地图没有语义标签，无法仅凭 occupied 判断它是墙体还是误点。关闭碰撞检查会同时放过真实结构障碍物。

**经验**：应在感知输入端过滤确定属于机体的点，在规划端保持墙体安全检查。

### 7.5 使用统一近距离盲区删除自点云

**方案**：把 `min_ray_length_m` 从 0.3 m 提高到 0.5 或 0.6 m。

**为什么不行**：它会同时删除靠近无人机的真实墙体和动态障碍物，直接损失近场安全能力。

**替代方案**：当前采用机身盒和旋翼圆柱的模型感知自滤波。

### 7.6 把 `clear_sphere_radius` 当作地图清除工具

**为什么不行**：`clear_sphere_for_planning` 主要影响规划查询区域，并不等价于从 TSDF 中删除历史高权重占据体素。动态障碍物移走后，如果没有新的自由射线穿过原体素，旧表面仍可能残留。

**经验**：区分规划查询放宽、TSDF ray carving、权重衰减和输入点云过滤四个层次。

### 7.7 普通 start recovery 覆盖所有根节点问题

**为什么不行**：旧 recovery 只在起点盒 occupied 时触发，而 `1 vertex / 0 edges` 常发生在起点盒 free、移动扫掠边 blocked 的状态。

**替代方案**：单独设计 blocked-root recovery。

### 7.8 提前规划路径提交前执行完整最新地图复检

**实现过的版本**：全局边界、unknown、碰撞、clearance 全流程复检。

**问题**：

- 与原规划阶段重复大量工作。
- 规划起点与实际提交时当前位置不同，容易发生起点不匹配。
- 地图伪占据使正确路径被二次否决。
- 复检失败后重新规划，形成“先快速提前规划，再长时间同步重规划”的反效果。
- 状态机需要处理缓存、异步复检、过期结果和重规划，复杂度显著上升。

**结果**：回退到“安全提前规划但无提交时复检”版本。

### 7.9 仅把复检放入异步 callback queue

**为什么不行**：异步化只能减少阻塞，不能修复碰撞判定对伪 occupied 过敏的正确性问题。错误拒绝即使更快，仍会导致后续重规划。

### 7.10 自适应提前量与复杂复检同时上线

**方案**：根据规划时间 P90 动态计算提前触发时间。

**优点**：理论上可适应规划延迟波动。

**回退原因**：与复检失败、缓存过期和同步重规划组合后，状态空间快速膨胀，调试成本超过当时收益。

**经验**：先稳定缓存路径生命周期和正确性，再引入延迟预测控制。

### 7.11 激进路径捷径化

**现象**：路径从 9 或 11 个顶点直接缩减到 3 个顶点。

**为什么不行**：虽然直线段碰撞检查可能通过，但大幅切弯会增加控制跟踪难度，并使路径更敏感于点云时延和局部地图误差。

**结果**：当前关闭 `final_shortcut_enable`，保留 RRG 拓扑，只执行小范围连续优化。

### 7.12 始终施加归一化 ESDF 推力

**现象**：开阔区域路径本已具有充分净空，优化器仍持续横向推动路径，净空略有提升但曲率明显恶化。

典型日志：

```text
clearance 1.646 -> 1.782
smoothness cost 0.0067 -> 0.0563
fallback=no_improvement
```

**结果**：改为 clearance-gated 力场，仅在低于目标净空时施加 ESDF 推力。

### 7.13 修改碰撞盒本身适配全局限高

**为什么不行**：缩小碰撞盒会改变真实安全边界，并掩盖采样、图连接和最终检查之间的坐标或边界语义不一致。

**正确做法**：保持真实碰撞盒，通过修复空中机器人高度投影和统一全局边界语义解决。

### 7.14 认为 RViz 点云稠密等同于当前 occupied

**为什么不完全成立**：RViz 可能显示原始点云、TSDF 表面点、累积地图或历史 Marker。视觉残留不一定对应当前碰撞查询结果。

**经验**：同时检查：

```text
/livox/lidar
/livox/lidar_fixed
/gbplanner_node/surface_pointcloud
起点盒状态
根边碰撞状态
```

## 8. 当前仍需改进的问题

### 8.1 连续短路径执行

当前 `short_frontier` 特例可能允许长度低于 `min_path_length` 的高密度 frontier 路径。建议增加：

- 短路径连续执行次数上限。
- 至少沿历史方向推进一定距离后才允许再次选择短 frontier。
- 将相邻多个短 frontier 合并为一个局部目标，再生成一次较长路径。
- 对低于最小执行时域的路径只作为恢复动作，不作为常规探索路径。

### 8.2 动态障碍物地图更新

当前 Voxblox 支持 ray carving，但不是完整动态占据概率模型。后续可增加：

- 体素时间戳和占据权重衰减。
- 动态点聚类与短时轨迹预测。
- 静态结构层和动态障碍层分离。
- 只对动态层执行快速清除，避免墙体地图被噪声侵蚀。

### 8.3 提前规划的预测起点

当前提前规划仍主要依赖当前执行状态和起点容差。后续可基于当前轨迹末端状态作为规划根节点，并加入版本化地图快照，减少缓存起点错位。

### 8.4 从路径优化升级到动力学轨迹优化

当前优化对象是离散几何路径点，不直接优化速度、加速度和 jerk。后续可以在安全路径走廊内使用 B-spline、minimum-snap 或基于凸安全走廊的轨迹优化，进一步提高跟踪平滑性。

## 9. 推荐的下一阶段验证方案

1. 完整重启规划节点，使机体自点云过滤生效并清空旧 Voxblox 地图。
2. 对比 `/livox/lidar`、`/livox/lidar_fixed` 和 `/livox/lidar_self_points`，确认仅机体点被剔除。
3. 统计过滤前后 `1 vertex / 0 edges`、`BLOCKED_ROOT` 和 `START_RECOVERY` 次数。
4. 统计连续短路径的长度分布、短 frontier 触发次数和单位时间净推进距离。
5. 暂不同时修改多个 RRG 参数，先判断自点云过滤是否已经改善根边连通性。
6. 若短路径仍占主导，优先修改 short-frontier 执行策略，而不是继续降低碰撞安全参数。

## 10. 简历写法

### 10.1 项目名称建议

```text
面向复杂空间自主探索的 GBPlanner3 安全增强与异步规划系统
```

或：

```text
基于 RRG、ESDF 与 Voxblox 的无人机低停顿自主探索规划优化
```

### 10.2 简历精炼版

- 基于 GBPlanner3 构建无人机安全增强探索框架，在局部 RRG 候选评价中融合 ESDF 净空、中心偏置、路径平滑和历史方向一致性约束，实现从二值避障到多目标安全路径选择的升级。
- 设计最小起点盒、dirty-root 容错与 blocked-root recovery 分层恢复机制，解决地图伪占据导致的起点状态振荡及 `1 vertex / 0 edges` 根节点失连问题，同时保留墙体、地板和天花板硬安全约束。
- 设计执行期异步提前规划与完成后原子切换机制，通过序列号、缓存时效、起点一致性和状态隔离防止旧路径抢占当前轨迹，降低规划执行串行等待。
- 实现受时间和偏移约束的 ESDF 局部路径优化器，将概率图搜索与连续路径优化解耦；优化失败自动回退安全原路径，实验路径最小净空由 `1.057 m` 提升至 `1.411 m`。
- 打通 Gazebo Livox、TF、Voxblox 和 RRG 数据链路，定位机身/旋翼自反射造成的近场伪占据，并实现基于真实机体几何的点云自滤波，避免扩大雷达统一盲区而损失近场障碍感知。
- 建立 RRG 候选排名、分阶段拒绝原因、RViz 状态文字和规划分段计时体系，将“规划卡住”定位到地图输入、根边连通、收益筛选、路径后处理或执行状态机等具体环节。

### 10.3 一段式项目描述

针对 GBPlanner3 在复杂点云环境中存在的贴墙探索、起点伪占据、RRG 根节点失连和规划执行串行停顿问题，设计并实现了基于 ESDF 的多目标安全路径评价、历史方向驱动的前方优先探索、blocked-root 分层恢复、执行期异步提前规划及受限局部路径优化。进一步打通 Gazebo Livox 到 Voxblox 的点云链路，使用真实机体几何剔除无人机自反射点，并通过 RViz 与结构化日志建立可解释规划诊断体系。在保持全局规划、时间/里程返航和墙体硬安全约束隔离的前提下，提高了局部探索连续性、路径净空和系统可调试性。

### 10.4 面试展开主线

可以按以下顺序讲述：

1. **问题定位**：无人机卡住不等于规划算法慢，日志证明常见根因是连续 empty path 或根节点所有第一条边被拒绝。
2. **地图层**：追踪 Gazebo 点云到 Voxblox，发现雷达会扫描机体和旋翼，统一距离过滤又会损失近场安全，因此采用几何自滤波。
3. **规划层**：增加稳定起点语义、blocked-root recovery 和 ESDF 多目标候选评价。
4. **路径质量层**：不再要求 RRG 同时承担拓扑搜索和连续平滑，而是在安全可行路径后增加有回退保证的快速优化器。
5. **系统层**：通过异步提前规划和原子切换隐藏规划延迟，并隔离全局规划与返航状态。
6. **工程方法**：对失败实验保留备份和日志证据，区分性能问题、正确性问题与状态机复杂度问题。

## 11. 技术关键词

```text
GBPlanner3
RRG / Random Geometric Graph
Voxblox TSDF / ESDF
PointCloud2
Model-aware Self Filtering
Collision Checking
Clearance-aware Planning
Multi-objective Path Scoring
Blocked-root Recovery
Asynchronous Preplanning
Atomic Trajectory Handoff
Local Path Optimization
ROS / Gazebo / RViz
PX4 SITL
```

## 12. 验证与备份说明

- 多轮执行 `catkin build planner_common gbplanner planner_control_interface pci_general` 均完成编译。
- 最新机体点云过滤执行 `catkin build gbplanner`，24 个相关包成功、无警告。
- 关键阶段均保存在 `config_backups/`，可按功能节点回退。
- 提前规划完整复检、自适应提前量和激进捷径化属于实验后回退功能，不应在简历中描述为当前在线能力。
- 动态障碍物完整预测与时序占据建模尚未实现，不应表述为已具备动态避障系统。
