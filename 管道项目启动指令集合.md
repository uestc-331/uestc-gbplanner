# 当前仓库仿真启动指令

当前仿真工作空间：

```bash
/home/uestc/gb3/uestc-gbplanner
```

## 一键启动推荐

先清理上一次残留进程：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --cleanup
```

启动 floor5 仿真、控制器、规划器和 RViz：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --env floor5 --rviz
```

如果只想打印每个终端要执行的命令：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --env floor5 --rviz --print
```

其他环境：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --env pipe --rviz
```

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --env pipeline_xyq --rviz
```

## 手动分终端启动

每个终端先执行：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
```

终端 1：启动 PX4 + Gazebo：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch px4 floor5.launch
```

终端 1：启动 PX4 + Gazebo 无图形化：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch px4 floor5.launch gui:=false verbose:=false
```


终端 2：真实位姿桥接：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch sim_tools get_real_pose.launch
```

终端 3：键盘遥控：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch sim_tools sim_remote.launch
```

终端 4：px4ctrl：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch px4ctrl run_ctrl.launch
```

终端 5：uav_control：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch uav_control setup.launch
```

终端 6：gbplanner + RViz：

注意：这一条只启动规划器和 RViz，不会启动 PX4/Gazebo/lidar。必须先启动终端 1-5，或者直接用上面的“一键启动推荐”，否则 RViz 可能只有黑色背景和少量按钮。

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch gbplanner rmf_sim.launch rviz_en:=true traj_server_en:=true
```

## 单独打开 RViz

注意：单独打开 RViz 只负责显示，不会产生仿真数据。若没有先启动 PX4/Gazebo/gbplanner，RViz 里看不到点云、地图和路径是正常的。

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
rviz -d /home/uestc/gb3/uestc-gbplanner/src/exploration/gbplanner_ros/gbplanner/config/rviz/rmf_obelix_gbplanner3_safe.rviz
```

## 起飞和开始规划

在 `sim_remote` 终端按：

```text
z
```

`z` 会先发布 `/mission_state=4`，再发布 `/px4ctrl/takeoff_land` 起飞命令。

起飞后开始自动规划：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
rosservice call /planner_control_interface/std_srvs/automatic_planning "{}"
```

停止规划：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
rosservice call /planner_control_interface/std_srvs/stop_planning "{}"
```

## 常用排查

检查 px4 包是否来自 PX4-Autopilot：

```bash
cd /home/uestc/gb3/uestc-gbplanner
source scripts/gb3_env.bash
rospack find px4
```

检查 gbplanner 是否来自当前仓库：

```bash
cd /home/uestc/gb3/uestc-gbplanner
source scripts/gb3_env.bash
rospack find gbplanner
```

同步 floor5/pipe/pipeline_xyq 仿真资源到 PX4：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
bash scripts/sync_px4_sim_assets.bash
```

单独清理残留进程：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/kill_current_sim.bash
```

或：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/run_current_sim.bash --cleanup
```

## 录包指令

```bash
rosbag record \
  /rosout \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate \
  /vis/planning_global_graph \
  /vis/planning_global_path \
  /vis/planning_graph \
  /vis/best_planning_paths \
  /vis/shortest_paths \
  /vis/planning_homing_path \
  /vis/imp_ref_path \
  /gbplanner_path \
  /rmf_obelix/command/trajectory \
  /pci_command_trajectory_vis \
  /position_cmd \
  /pci_carrot \
  /pci_carrot_point \
  -O 202606
```
