# 仿真迁移文件清单

为了在另一台电脑上复现当前仿真，本仓库已经补齐以下关键内容。

## 1. 已补入当前工作空间的 ROS 控制包

以下两个包原来只在 `/home/uestc/csj/uestc-gb3` 里，现在已经复制到当前仓库：

```text
src/px4ctrl
src/uav_control
```

作用：

- `px4ctrl`：订阅 `/position_cmd` 和里程计，输出 PX4 控制指令。
- `uav_control`：仿真控制链路中的任务/模式辅助节点。

这样新电脑上不需要再 source 旧的 `csj/uestc-gb3` 工作空间。

## 2. 已补入仓库的 PX4/Gazebo 仿真资产

PX4 相关的小型自定义仿真文件放在：

```text
external/px4_sim_assets
```

包含：

```text
external/px4_sim_assets/launch
external/px4_sim_assets/worlds
external/px4_sim_assets/models
```

主要文件：

```text
launch/floor5.launch
launch/pipe.launch
launch/pipeline_xyq.launch
launch/single_vehicle_spawn_xtd.launch

worlds/floor5_5_8.world
worlds/floor5.world
worlds/pipe.world
worlds/pipe_test_1.world
worlds/pipeline_xyq.world

models/floor5
models/floor5_5_8
models/pipe_1200mm
models/pipe_test_1
models/pipeline_xyq
models/iris_csj
models/asphalt_plane
```

其中 `pipe_test_1` 原来在 `~/.gazebo/models/pipe_test_1`，不是 PX4 目录里的文件，也已经收进仓库。

## 3. 新电脑同步方法

先准备 PX4，例如放在：

```bash
~/PX4_Firmware
```

然后在本仓库根目录执行：

```bash
cd ~/uestc-gbplanner
PX4_FIRMWARE_ROOT=~/PX4_Firmware bash scripts/sync_px4_sim_assets.bash
```

这个脚本会把：

```text
external/px4_sim_assets/launch  ->  PX4_Firmware/launch
external/px4_sim_assets/worlds  ->  PX4_Firmware/Tools/sitl_gazebo/worlds
external/px4_sim_assets/models  ->  PX4_Firmware/Tools/sitl_gazebo/models
```

## 4. 编译与启动

新电脑上建议只 source 当前工作空间：

```bash
cd ~/uestc-gbplanner
source scripts/gb3_env.bash
catkin build
```

启动前检查不要混入旧路径：

```bash
env | grep -E 'csj|uestc-gb3'
```

如果没有输出，再启动仿真和规划。

```bash
roslaunch px4 floor5.launch
roslaunch px4ctrl run_ctrl.launch
roslaunch uav_control setup.launch
roslaunch gbplanner rmf_sim.launch rviz_en:=true traj_server_en:=true
```

## 5. 注意

- 不要在启动 `gbplanner/RViz` 的终端 source 旧的 `/home/uestc/csj/uestc-gb3/devel/setup.bash`。
- `external/px4_sim_assets` 只保存自定义小资产，不包含完整 PX4 工程。
- 新电脑仍需要安装 ROS Noetic、Gazebo、MAVROS，并准备可正常编译运行的 PX4 工程。
