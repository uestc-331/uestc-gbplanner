# 快速碰撞复检改造前备份

备份时间：2026-07-16。

本目录保存以下改造前的文件：

- 缓存路径按当前位置裁剪并替换旧规划根节点。
- 最新地图复检只执行碰撞检查，不重复计算 clearance。
- 复检使用 `size + size_extension_min` 最小安全盒，不使用 Extended 大盒。

路径生成阶段的离墙评分、最终安全检查和 Global 硬边界保持不变。
