# 20260515 实机飞行问题分析

分析对象：

- bag: `/home/super/uestcgbplanner3/202605152046.bag`
- ROS 日志: `/home/super/.ros/log/7b2c04d8-505b-11f1-8996-6b06d0fc08a1/rosout.log`
- 主要配置: `src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/planner_control_interface_real_config.yaml`

## 结论

这次飞行中，距离返回确实触发了，但返航路径规划失败，所以没有真正执行返航。

关键结论：

1. 按下 `1` 后启动慢，主要慢在 gbplanner / PCI 生成第一条可执行轨迹之前，不是 `/position_cmd` 发布慢。
2. 中间有一次明显前冲，发生在第三段轨迹末端附近，实际跟踪超调约 `0.6 m`，实际速度峰值约 `1.5 m/s`。
3. 距离返回确实触发，日志明确显示 `[PCI][DIST_BUDGET] trigger homing`。
4. 返航失败的直接原因是 homing path 被判定不安全，所有 fallback 都失败，最终返回空路径。
5. bag 结束前没有录到“指令让飞机往下摔”的证据；最后 `/position_cmd` 仍然是悬停指令。

## 时间线

bag 时间范围：

```text
2026-05-15 20:50:07.38 -> 20:52:02.84
duration: 115 s
```

主要话题：

```text
/vins_fusion/imu_propagate              7178 msgs
/position_cmd                           8814 msgs
/rmf_obelix/command/trajectory             4 msgs
/gbplanner_is_homing                       4 msgs
/gbplanner_path                            4 msgs
```

## 现象 1：按下 1 后规划很慢

bag 中第一次规划可视化出现较早：

```text
t=15.440s /vis/planning_graph
t=15.630s /vis/shortest_paths
t=16.720s /vis/planning_global_graph
```

但真正发出可执行轨迹较晚：

```text
t=27.324s /rmf_obelix/command/trajectory
t=27.325s /position_cmd
```

也就是说，从规划图开始出现到真正发控制指令，中间大约有 `11.9 s`。

判断：

- 慢点不在 `traj_server_node`。
- `traj_server_node` 收到 `/rmf_obelix/command/trajectory` 后几乎立刻开始发布 `/position_cmd`。
- 主要慢在 gbplanner / PCI 生成第一条有效执行轨迹之前。

结合 rosout 日志，可以把第一次启动慢拆得更清楚：

```text
1778849420.562  Switch to auto mode / Start Planner
1778849420.702  PCI Planning iteration 0
1778849420.832  GBPlanner service request seq=0
1778849420.833  RRG buildGraph begin, loops_max=200
1778849422.818  graph formed
1778849423.011  Path ACCEPTED
1778849423.405  Post-processed path failed collision check, returning empty path
1778849423.405  PCI immediately starts Planning iteration 1
1778849424.097  GBPlanner service request seq=1
1778849424.101  RRG buildGraph begin, loops_max=260
1778849432.065  graph formed
1778849432.999  Path ACCEPTED
1778849434.628  Compute an alternate path in 1.617606s
1778849434.687  GBPlanner service response path_size=17
1778849434.705  PCI starts executing wp=1/129
```

所以这次“按下 1 后很慢”的直接原因不是控制端卡住，而是：

1. 第一轮局部探索确实找到了候选路径，但 `path_safety_enhance` 后处理后的路径没有通过碰撞检查，最后返回了空路径；
2. PCI 收到空路径后马上重试第二轮；
3. 第二轮 `buildGraph` 花了大约 `7.96s`，后面的 path safety / alternate path 又花了约 `1.62s`；
4. 第二轮成功后，PCI 到真正执行只用了十几毫秒。

也就是说，慢主要来自 `gbplanner_node` 内部的 RRG 采样建图、路径筛选和安全增强，不是 `/rmf_obelix/command/trajectory` 到 `/position_cmd` 的执行链路慢。

当前配置里和这件事最相关的是：

```yaml
PlanningParams:
  exp_sensor_list: ["OS064"]
  edge_length_max: 0.8
  num_vertices_max: 200
  num_loops_max: 600
  nearest_range: 2.0
  path_safety_enhance_enable: true
  free_frustum_before_planning: true
  cluster_vertices_for_gain: true
```

其中 `OS064` 是 360 度激光视场，候选点增益评估范围大；`num_loops_max` / `num_vertices_max` 决定采样搜索量；`path_safety_enhance_enable` 会对路径再做安全修正和碰撞复查。第二轮日志里的 `loops_max=260`、`vertices_max=2600` 说明实际运行时还会根据状态扩大搜索量。

建议解决方案：

1. 先解决“第一轮找到路径但后处理后碰撞失败”的问题。这个和返航失败类似，都指向地图/膨胀/路径后处理碰撞判断偏紧。重点看 RViz 里的 `/vis/ref_path`、`/vis/mod_path`、`/vis/occupied_pcl`、`/gbplanner_node/tsdf_pointcloud`。
2. 短期实机测试可以先关闭或弱化安全增强验证一次规划速度：

```yaml
PlanningParams:
  path_safety_enhance_enable: false
```

只建议在低速、空旷、小范围测试中验证，不建议直接带这个配置做复杂环境实飞。

3. 如果关闭 safety enhance 后速度明显变快，说明主要卡在路径后处理/碰撞复查；下一步应调小 `RobotParams.size_extension`、`safety_extension`，或者修正地图误障碍，而不是长期关闭安全检查。
4. 如果关闭后仍慢，再降低采样量：

```yaml
PlanningParams:
  num_vertices_max: 120
  num_loops_max: 300
  nearest_range: 1.2
```

这会明显加快，但代价是路径质量和成功率可能下降。

5. 对实机第一次启动，建议先不要刚起飞就马上按 `1`，让局部地图稳定 2 到 3 秒；如果刚启动地图还在快速更新，第一轮路径更容易被后处理判碰撞，从而触发重试。

## 现象 2：中间猛地往前一走

最明显的超调发生在：

```text
t ~= 77.5s -> 78.5s
trajectory_id = 2
```

当时最大跟踪误差约：

```text
err ~= 0.60 m
```

典型数据：

```text
t=78.070s
odom=(14.34, 5.99, 0.76)
cmd =(13.82, 6.23, 0.91)
```

实际里程计速度峰值：

```text
max odom speed ~= 1.50 m/s
```

而指令速度上限约：

```text
cmd speed max ~= 1.00 m/s
```

同时 `/position_cmd` 在一处出现较硬的速度变化：

```text
t=77.865s -> 77.875s
vel (-0.16, 0.53, 0.77) -> (0.00, 0.00, 0.01)
10 ms 内速度变化约 0.94 m/s
```

判断：

- 这更像是轨迹末端/切换处不够平滑，加上实机控制跟踪超调。
- 当前实机参数偏激进：

```yaml
RobotDynamics:
  v_max: 1.0
  a_max: 4.0
```

建议实机先降保守：

```yaml
RobotDynamics:
  v_max: 0.5
  v_init_max: 0.5
  v_homing_max: 0.5
  a_max: 1.0
  dt: 0.05
```

## 现象 3：距离返回是否触发

确认触发了。

配置中：

```yaml
distance_budget_enable: true
distance_budget_limit: 12.0
distance_budget_max_step: 2.0
distance_budget_min_step: 0.01
distance_budget_release_timeout: 3.0
```

日志中明确显示：

```text
2026-05-15 20:42:27
[PCI][DIST_BUDGET] config enable=1 limit=12.00 ...

2026-05-15 20:44:19
[PCI][DIST_BUDGET] reset reason=AUTO start limit=12.00

2026-05-15 20:51:16
[PCI][DIST_BUDGET] accumulated=11.81/12.00 ...

2026-05-15 20:51:17
[PCI][DIST_BUDGET] trigger homing accumulated=12.02 limit=12.00 step=0.069 -> wait for current path to finish then run direct homing
```

另外，用 bag 里的 `/vins_fusion/imu_propagate` 按相同规则反算，累计距离约：

```text
18.92 m
```

超过 `12.0 m` 的时间约：

```text
t=71.555s
accumulated=12.01 m
```

## 为什么触发了返回却没回去

日志给出的直接原因是：homing path 规划失败，返回了空路径。

距离返回触发后，PCI 先等待当前路径结束：

```text
2026-05-15 20:51:17
[PCI][DIST_BUDGET] homing pending; waiting for current path to finish accumulated=12.02 limit=12.00
```

等待 3 秒超时后强制 homing：

```text
2026-05-15 20:51:20
[PCI][DIST_BUDGET] release timeout reached; forcing homing now accumulated=12.02 limit=12.00 timeout=3.00

2026-05-15 20:51:20
Homing through direct service call
```

随后 gbplanner 尝试计算返航路径，但失败：

```text
2026-05-15 20:51:22
[RRG][HOMING] Post-processed homing path failed collision check, trying raw homing path as fallback.
[RRG][HOMING] Raw homing path is also unsafe, trying emergency fallbacks.
[RRG][HOMING] Emergency fallback failed too; returning empty path.
PCI: homing path not executed (execute_path=true, returned_poses=0)
```

手动返回也一样失败：

```text
2026-05-15 20:51:54
[GBPLANNER-SIMPLE-UI] Homing

2026-05-15 20:51:58
[RRG][HOMING] Emergency fallback failed too; returning empty path.
PCI: homing path not executed (execute_path=true, returned_poses=0)
```

判断：

- 距离返回不是没触发。
- 手动返回也不是没触发。
- 真正的问题是返航路径被碰撞检查判定不安全，raw path 和 fallback 都失败。
- 因为返回路径为空，PCI 没有执行返航路径。

## 为什么会悬停在那里

bag 后段 `/position_cmd` 一直是原地悬停：

```text
pos ~= (13.78, 6.37, 1.08)
vel = (0, 0, 0)
```

最后 20 秒，里程计高度也基本保持：

```text
odom z ~= 1.05 -> 1.11
```

所以 bag 内没有看到“规划器发下降指令”的证据。

更准确地说：

- 距离返回触发后，homing path 失败并返回空路径。
- PCI 没有新的有效返航轨迹可执行。
- 控制端继续保持原地 setpoint / 悬停 setpoint。
- 所以看起来就是停在原地不回家。

## 关于最后坠机

当前 bag 内没有直接证据说明“坠机是由 `/position_cmd` 下发下降指令导致的”。

bag 结束前：

```text
/position_cmd: z ~= 1.08, vel_z = 0
/vins_fusion/imu_propagate: z ~= 1.05 ~ 1.11
```

因此最后坠机更可能发生在 bag 结束之后，或者发生在 bag 没有录到的低层控制/飞控链路里。

可能原因包括：

- offboard setpoint 丢失
- 飞控 failsafe
- 定位/VINS/LIVO 状态异常
- ROS 节点或网络中断
- 电源/通信问题
- 低层控制器没有继续收到有效 setpoint

但这次 bag 没有录 `/mavros/state`、`/mavros/setpoint_raw/local`、飞控模式、低层 thrust/attitude 等话题，所以不能定死坠机原因。

## 下一步建议

### 1. 实机速度先降保守

建议先把 `planner_control_interface_real_config.yaml` 里的执行速度和加速度降下来：

```yaml
RobotDynamics:
  v_max: 0.5
  v_init_max: 0.5
  v_homing_max: 0.5
  a_max: 1.0
  dt: 0.05
```

### 2. 下次必须录 rosout 和飞控状态

建议录包加上：

```bash
/rosout
/gbplanner_is_homing
/rmf_obelix/command/trajectory
/position_cmd
/vins_fusion/imu_propagate
/mavros/state
/mavros/local_position/odom
/mavros/setpoint_raw/local
/mavros/imu/data
```

### 3. 单独测试 homing path

在飞远前，先测试一次手动返回服务是否能生成非空路径。

需要重点看日志中是否出现：

```text
PCI: homing planner returned X poses
PCI: executing homing path with X poses
```

如果出现：

```text
returned_poses=0
Emergency fallback failed too
```

说明返航路径仍然不可用，不能继续做实机距离返回测试。

### 4. 检查返航路径为什么被判不安全

本次失败日志说明：

```text
Post-processed homing path failed collision check
Raw homing path is also unsafe
Emergency fallback failed too
```

需要重点检查：

- home 点是否正确设置
- global graph 是否正确记录了来时路径
- 地图中是否把来时路径附近错误标成 occupied/unknown
- `RobotParams.size_extension` / `safety_extension` 是否过大
- homing 碰撞检查是否对 unknown voxel 过于保守
- 点云/TF 是否有偏移，导致路径被误判碰撞

更准确地说，代码里的返航路径检查分成几层：

1. 先检查 safety-enhance / post-process 之后的返航路径；
2. 如果失败，再检查原始 raw homing path；
3. 如果 raw 也失败，再用 emergency fallback 检查：
   - `min/strict`
   - `exact/strict`
   - `min/allow_unknown`
   - `exact/allow_unknown`

其中 `strict` 表示 unknown voxel 也不允许通过；`allow_unknown` 表示 unknown 可以放行。

因此，如果日志最终出现：

```text
Emergency fallback failed too; returning empty path.
PCI: homing path not executed (execute_path=true, returned_poses=0)
```

说明即使用更小的机器人包络、甚至允许 unknown voxel 通过，路径仍然没有通过地图碰撞检查。这个现象更像是某一段返航路径在 voxblox 地图里被判断为 occupied / collision，而不是单纯因为 unknown 区域太保守。

从日志看，前面有一次 `Calling HOMING instead` 时，emergency fallback 曾经成功：

```text
Emergency fallback succeeded: exact/strict
```

但距离触发返航那次，以及后面手动返航那次，都是：

```text
Post-processed homing path failed collision check
Raw homing path is also unsafe
Emergency fallback failed too; returning empty path.
```

所以结论是：返航机制确实被触发了，但触发时生成的返航路径在当前地图状态下被碰撞检测否决，PCI 收到的是 0 个 pose，于是没有真正执行返航轨迹。
