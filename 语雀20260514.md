# gbplanner3 实机探索规划调试阶段总结

日期：2026-05-14  
工作空间：`/home/super/uestcgbplanner3`

## 1. 当前进展

这几天已完成 gbplanner3 在实机链路上的初步打通。当前已经可以通过 ROS 话题方式控制无人机进行探索规划调试，包括启动规划、停止规划、手动回家等基础操作。

目前已验证的主要链路：

```text
终端/话题命令
  -> /gbplanner_ui/cmd/...
  -> gbplanner_topic_command_bridge
  -> planner_control_interface
  -> gbplanner
  -> /rmf_obelix/command/trajectory
  -> /position_cmd
  -> 控制端
```

已重点解决的问题：

- 点云更新队列过满。
- 路径更新慢、规划响应慢。
- 实机环境下通过话题触发初步探索规划。
- 手动发送 homing 命令后，可通过话题观察是否进入回家状态。

## 2. 当前主要参数调整

### 2.1 探索规划范围

配置文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml
```

当前局部规划范围已收紧：

```yaml
BoundedSpaceParams:
  Local:
    min_val: [-7.5, -7.5, -7.0]
    max_val: [7.5, 7.5, 7.0]
```

作用：

- 减少单次规划采样空间。
- 降低无效采样和碰撞检测开销。
- 缓解路径更新慢的问题。

### 2.2 图搜索规模

当前参数：

```yaml
num_vertices_max: 200
num_edges_max: 2000
num_loops_cutoff: 500
num_loops_max: 600
```

作用：

- 相比过大的搜索规模，降低单次规划计算量。
- 保留一定搜索充分性，避免路径质量过差。
- 主要用于平衡“规划速度”和“路径质量”。

### 2.3 路径形状和平滑相关

当前参数：

```yaml
edge_length_max: 0.8
path_interpolation_distance: 0.4
yaw_tangent_correction: true
path_safety_enhance_enable: true
```

作用：

- `edge_length_max` 控制 RRG 单条边最大长度，减小后路径点更密，折线感会降低。
- `path_interpolation_distance` 控制路径插值间距，减小后执行端接收到的路径更连续。
- `yaw_tangent_correction` 让机体 yaw 尽量沿路径切线方向，减少朝向突变。
- `path_safety_enhance_enable` 用于路径安全增强，主要提升避障安全性。

### 2.4 点云和地图更新

配置文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/voxblox_sim_config.yaml
```

当前关键参数：

```yaml
max_ray_length_m: 15.0
pointcloud_queue_size: 5
min_time_between_msgs_sec: 0.20
publish_traversable: False
publish_tsdf_map: False
publish_esdf_map: False
```

作用：

- 降低点云积分压力。
- 控制点云进入地图模块的频率。
- 关闭部分重型地图发布，减少 CPU 和带宽占用。
- 缓解 “pointcloud queue getting too long” 和路径更新慢的问题。

### 2.5 执行轨迹参数

配置文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/planner_control_interface_real_config.yaml
```

当前关键参数：

```yaml
smooth_heading_enable: true
interpolate_traj: true
use_time_from_start: true

RobotDynamics:
  v_max: 1.0
  v_homing_max: 1.0
  yaw_rate_max: 0.1
  dt: 0.05
  a_max: 4.0
```

作用：

- `smooth_heading_enable` 平滑执行路径 yaw。
- `interpolate_traj` 对路径点插值生成轨迹。
- `v_max / yaw_rate_max / a_max / dt` 共同影响控制端执行轨迹的速度、转向和加速度平滑性。

## 3. 当前调试话题

### 3.1 话题控制

常用话题：

```bash
rostopic pub -1 /gbplanner_ui/cmd/start_planner std_msgs/Empty "{}"
rostopic pub -1 /gbplanner_ui/cmd/stop_planner std_msgs/Empty "{}"
rostopic pub -1 /gbplanner_ui/cmd/homing std_msgs/Empty "{}"
rostopic pub -1 /gbplanner_ui/cmd/init_motion std_msgs/Empty "{}"
```

### 3.2 回家状态观察

```bash
rostopic echo /gbplanner_is_homing
```

含义：

```text
true  = 当前执行回家路径
false = 当前不是回家路径
```

### 3.3 关键路径观察

```bash
rostopic echo /vis/ref_path
rostopic echo /gbplanner_path
rostopic echo /rmf_obelix/command/trajectory
rostopic echo /position_cmd
```

## 4. 建议录包

### 4.1 关键版

```bash
mkdir -p ~/gbplanner_bags

rosbag record --lz4 --split --duration=5m \
  -O ~/gbplanner_bags/gbplanner_key_debug \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate
```

### 4.2 详细版

```bash
mkdir -p ~/gbplanner_bags

rosbag record --lz4 --split --duration=5m \
  -O ~/gbplanner_bags/gbplanner_full_debug \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate \
  /vis/planning_global_graph \
  /vis/planning_global_path \
  /vis/planning_graph \
  /vis/best_planning_paths \
  /vis/shortest_paths \
  /vis/planning_homing_path \
  /vis/imp_ref_path \
  /gbplanner_path \
  /rmf_obelix/command/trajectory \
  /pci_command_trajectory_vis \
  /position_cmd
```

## 5. 当前遗留问题

### 5.1 路径平滑性仍不理想

当前探索路径已经可以生成并下发，但路径几何和平滑执行仍需继续优化。后续重点关注：

- `edge_length_max`
- `nearest_range`
- `path_interpolation_distance`
- `smooth_heading_enable`
- `yaw_rate_max`
- `a_max`
- `v_max`

### 5.2 还未在五楼环境测试

目前还没有在五楼真实环境完成系统性测试。需要验证：

- 点云更新是否稳定。
- 定位话题是否连续可靠。
- 探索路径是否能持续更新。
- homing 是否能在真实环境中稳定触发和执行。

### 5.3 还未在真实管道环境测试

当前尚未进入真实管道场景测试。后续需要重点验证：

- 狭窄空间下路径安全性。
- 点云遮挡和稀疏情况下的地图构建。
- frontier 选择是否合理。
- 回家路径是否始终可用。

### 5.4 还未实现 UI 直接给话题

目前主要通过终端 `rostopic pub` 方式控制。后续需要把常用命令接入 UI，降低现场操作复杂度。

## 6. 下一步计划

1. 继续优化路径平滑性，优先调整路径几何和 PCI 执行轨迹参数。
2. 在五楼环境进行完整实机探索测试。
3. 在真实管道环境测试点云、地图、frontier、homing 的稳定性。
4. 完成 UI 到 `/gbplanner_ui/cmd/...` 话题的控制入口。
5. 固化录包流程，形成可复盘的实机测试数据集。

