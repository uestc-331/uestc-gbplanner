# 定位器
fastlivo

# 规划器

开启规划器

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch
```

启动命令行 UI

再开一个终端执行：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
rosrun gbplanner_ui gbplanner_simple_topic_ui
```

# 控制器

三个终端

```bash
cd pipe/
sss
px4ctrl 
```

```bash
cd pipe/
sss
uav
```

```bash
cd pipe/
sss
bash ./shfiles/rspx4.sh
```

# 录包指令（目前是规划器要用这些）

```bash
rosbag record \
  /rosout \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate \
  /vis/planning_global_graph \
  /vis/planning_global_path \
  /vis/planning_graph \
  /vis/best_planning_paths \
  /vis/shortest_paths \
  /vis/planning_homing_path \
  /vis/imp_ref_path \
  /gbplanner_path \
  /rmf_obelix/command/trajectory \
  /pci_command_trajectory_vis \
  /position_cmd \
  /pci_carrot \
  /pci_carrot_point \
  -O 202606
```


/rmf_obelix/base_link \

## 录包话题说明

- `/rosout`：ROS 日志。用来看规划器、PCI、TF、voxblox 等节点当时有没有报错、卡住、触发返航、规划失败等。
- `/gbplanner_is_homing`：规划器是否处于返航/homing 状态。排查“是不是突然开始回家”很关键。
- `/vis/ref_path`：最终参考路径可视化，一般是规划器/控制接口准备执行或展示的主路径。
- `/gbplanner_node/surface_pointcloud`：voxblox/gbplanner 发布的表面点云地图。用来看管道、障碍物、地图高度和路径相对位置。
- `/vins_fusion/imu_propagate`：定位里程计，记录无人机实际位置、姿态、速度。后处理时判断实际飞行轨迹和高度主要靠它。
- `/vis/planning_global_graph`：全局图可视化，包含全局图节点/边以及 frontier 信息。用来看全局探索方向和候选前沿点。
- `/vis/planning_global_path`：全局路径可视化，通常用于看当前位置到全局目标/返航路径的连接情况。
- `/vis/planning_graph`：本地 RRG/局部规划图。用来看本地采样、连边、探索树有没有往异常方向扩展。
- `/vis/best_planning_paths`：规划器评估出的较优候选路径集合。用来对比“为什么选了当前这条路”。
- `/vis/shortest_paths`：候选点到当前点的最短路径可视化。用来看图搜索连通性和路径长度。
- `/vis/planning_homing_path`：规划器计算出的返航路径。排查自动返航、安全预算触发时很有用。
- `/vis/imp_ref_path`：改进/增强后的参考路径可视化，通常用于观察路径安全增强、平滑或局部修正后的结果。
- `/gbplanner_path`：gbplanner 输出的规划路径消息。后处理时比纯 RViz marker 更方便解析路径点。
- `/rmf_obelix/command/trajectory`：发给轨迹执行/控制侧的 trajectory。判断“规划路径”和“实际下发轨迹”是否一致要看它。
- `/pci_command_trajectory_vis`：PCI 生成的控制轨迹可视化。用来在 RViz 中看 PCI 实际准备执行的轨迹形状。
- `/position_cmd`：最终给底层控制器的位置指令。判断控制端到底收到什么目标点/速度，要看这个。
- `/pci_carrot`：PCI 路径跟踪中的 carrot 目标位姿，表示当前跟踪的前瞻目标。
- `/pci_carrot_point`：PCI carrot 目标点版本，便于 RViz 直接看当前跟踪点在哪里。
