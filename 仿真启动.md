# gbplanner3 启动步骤

本文档记录 `gbplanner3` 配合 `fastlivo` 的实机规划探索启动流程。

## 1. 启动前确认

先启动并确认 `fastlivo` 正常运行。gbplanner3 当前依赖下面两个输入：

```bash
rostopic info /cloud_registered_360
rostopic info /vins_fusion/imu_propagate
```

期望状态：

- `/cloud_registered_360` 类型为 `sensor_msgs/PointCloud2`，由 fastlivo 的 `/laserMapping` 发布。
- `/vins_fusion/imu_propagate` 类型为 `nav_msgs/Odometry`，用于 gbplanner3 的里程计输入。

## 2. 启动 gbplanner3

推荐直接使用脚本启动：

```bash
/home/super/uestcgbplanner3/scripts/gbplanner.sh
```

该脚本等价于：

```bash

## 规划器
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch


## ui
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
rosrun gbplanner_ui gbplanner_cli
```

## 3. 当前点云链路

gbplanner3 当前接入的点云链路是：

```text
/cloud_registered_360
  -> /pointcloud_real_robot_converter_node
  -> /cloud_registered_fixed
  -> /gbplanner_node
```

启动后检查订阅关系：

```bash
rostopic info /cloud_registered_360
rostopic info /cloud_registered_fixed
```

期望状态：

- `/cloud_registered_360` 的 Subscribers 里有 `/pointcloud_real_robot_converter_node`。
- `/cloud_registered_fixed` 的 Publishers 里有 `/pointcloud_real_robot_converter_node`。
- `/cloud_registered_fixed` 的 Subscribers 里有 `/gbplanner_node`。

## 4. 当前里程计链路

gbplanner3 当前接入的里程计链路是：

```text
/vins_fusion/imu_propagate
  -> /rmf_obelix/odo_throttler
  -> /rmf_obelix/ground_truth/odometry_throttled
  -> /gbplanner_node
  -> /pci_general_ros_node
```

启动后检查：

```bash
rostopic info /vins_fusion/imu_propagate
rostopic info /rmf_obelix/ground_truth/odometry_throttled
```

## 5. 操控 UI

当前推荐使用命令行 UI：

```bash
rosrun gbplanner_ui gbplanner_cli
```

如果使用 `/home/super/uestcgbplanner3/scripts/gbplanner.sh` 启动，脚本会自动打开该 CLI。

菜单含义：

```text
1) Start Planner
2) Stop Planner
3) Homing
4) Initialization
5) Go To Waypoint
6) Global Planner
0) Exit
```

常用操作：

- 输入 `1`：开始自动规划探索。
- 输入 `2`：停止规划。
- 输入 `3`：触发返航。
- 输入 `0`：退出 CLI。

## 6. 规划输出

gbplanner3 输出给控制端的轨迹链路是：

```text
/rmf_obelix/command/trajectory
  -> /traj_server_node
  -> /position_cmd
```

检查命令：

```bash
rostopic info /rmf_obelix/command/trajectory
rostopic info /position_cmd
```

如果输入 `1` 后规划正常，应该能看到 `/position_cmd` 有输出。

## 7. 常见问题检查

### `/cloud_registered_360` 没有订阅者

检查转换节点是否启动：

```bash
rosnode list | grep pointcloud
```

应看到：

```text
/pointcloud_real_robot_converter_node
```

如果没有，重新构建并启动：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
catkin build gbplanner
roslaunch gbplanner rmf_real.launch
```

### CLI 无法启动

重新构建 UI：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
catkin build gbplanner_ui
```

然后再运行：

```bash
rosrun gbplanner_ui gbplanner_cli
```

### 无规划输出

依次检查：

```bash
rostopic hz /cloud_registered_360
rostopic hz /cloud_registered_fixed
rostopic hz /vins_fusion/imu_propagate
rostopic hz /rmf_obelix/ground_truth/odometry_throttled
rostopic hz /position_cmd
```

如果 `/cloud_registered_fixed` 没有数据，优先检查点云转换节点。

如果 `/rmf_obelix/ground_truth/odometry_throttled` 没有数据，优先检查 fastlivo 里程计话题 `/vins_fusion/imu_propagate`。

## 8. 备用 RViz Topic UI

如果需要 RViz 按钮式 UI，可以使用：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real_topic_ui.launch
```

但当前推荐优先使用 `gbplanner_cli`，它最接近 gbplanner2 的操作方式。
