# 20260709-10 GBPlanner 调参、远离墙体和起点稳定性改造总结

## 20260710 追加总结

今天主要处理的是昨天遗留的两个问题：

1. 无人机明明在空旷位置，但规划器偶尔仍认为起点附近 `occupied`，随后进入 recovery，最后反复 empty path。
2. 路径仍可能贴墙、弯绕，且有时 `startPlanner` 后等待较久。

今天真正已经生效、并通过日志确认的改进是：**把“起点是否干净”的判断从大规划盒子里剥离出来，改成固定使用最小起点检查盒子**。这样 `bound=0/1/2` 切换时，不会一会儿用大盒子判 occupied、一会儿用小盒子判 free，减少误进入起点恢复逻辑。

### 今天新增/生效的关键改进

#### 1. 固定最小起点检查盒

修改位置：

- `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp`
- `src/exploration/gbplanner_ros/gbplanner/include/gbplanner/rrg.h`
- `src/exploration/gbplanner_ros/gbplanner/src/gbplanner.cpp`

当前起点检查不再直接使用 `robot_box_size_`，而是固定使用：

```text
RobotParams.size + RobotParams.size_extension_min
```

当前参数下约为：

```text
[0.35, 0.35, 0.35] + [0.25, 0.25, 0.20] = [0.60, 0.60, 0.55]
```

日志中已经确认生效：

```text
[RRG][START_CHECK_BOX] status=free ... size=[0.60 0.60 0.55] planning_size=[1.00 1.00 0.80]
[RRG][START_CHECK_BOX] status=free ... size=[0.60 0.60 0.55] planning_size=[0.80 0.80 0.67]
[RRG][START_CHECK_BOX] status=free ... size=[0.60 0.60 0.55] planning_size=[0.60 0.60 0.55]
```

这说明：不管 PCI 请求 `bound=0/1/2`，起点检查盒都稳定是 `[0.60, 0.60, 0.55]`。

#### 2. 起点最小盒为 free 时跳过 start recovery

新增函数：

```cpp
Rrg::isStartClearWithMinBound()
```

作用：专门用最小盒检查当前位置。如果它是 free，就不再调用 `getStartRecoveryPath()`。

日志中已经确认：

```text
[RRG][START_CHECK_MIN] status=free center=[...] size=[0.60 0.60 0.55]
[GBPLANNER][EXPLORE] start recovery skipped: min-bound start is free
[GBPLANNER][LOCAL] start recovery skipped: min-bound start is free
```

这解决的是之前的老问题：

```text
起点小盒其实 free
-> 仍然进入 START_RECOVERY
-> START_RECOVERY failed
-> Planner returned an empty path
```

今天这次日志里没有再看到：

```text
START_RECOVERY requested
START_RECOVERY failed
```

所以“起点 free 还误进 recovery”的老毛病基本被压住了。

#### 3. searchPathToPassGate 后恢复 bound mode

之前怀疑 `searchPathToPassGate()` 内部循环切换 `bound_mode` 后，没有恢复原来的 `robot_box_size_`，可能污染后续规划。

今天已增加保存/恢复逻辑：

```text
进入 searchPathToPassGate 前保存 bound_mode 和 robot_box_size_
函数结束前恢复
```

新增日志：

```text
[RRG][BOUND_MODE] restored after gate search size=[...]
```

作用：避免 gate search 把后续正常规划的碰撞盒状态带偏。

#### 4. dirty root escape / dirty start edge 容错

为了避免“根节点附近一点点脏体素”导致整条路径直接失败，加入过两个容错逻辑：

- `dirty_start_edge_tolerance_enable`
- `dirty_root_escape_enable`

当前日志中能看到它们在工作：

```text
[RRG][DIRTY_START] ... accepted root edge after trimming ...
[RRG][DIRTY_ROOT_ESCAPE] ... accepted dirty root prefix escape ...
```

含义是：如果路径开头附近因为地图噪声或自身点云被判 occupied，但离开起点一段距离后路径是安全的，就允许裁掉/忽略这段脏前缀，避免一直卡死在原地。

### 今天重要备份

今天新增的关键备份目录包括：

```text
config_backups/rmf_obelix_before_restore_bound_mode_after_gate_search_20260710/
config_backups/rmf_obelix_before_stable_start_check_recovery_gate_20260710/
config_backups/docs_before_20260710_summary_update/
```

其中 `rmf_obelix_before_stable_start_check_recovery_gate_20260710/` 备份了：

```text
rrg.cpp
rrg.h
gbplanner.cpp
README.md
```

### 当前这次日志的结论

这次不再是老的 start recovery 卡死问题，新的主要问题变成：

```text
候选路径太短，被 min_path_length 过滤
路径安全增强 improveFreePath 尝试修改路径后，部分修改结果不 collision-free
```

关键日志：

```text
evaluated=4 positive=4 pass_gain=4/4 pass_length=0/4 pass_density=4/4 pass_direction=4/4 pass_forward=4/4 pass_all=0/4
```

候选路径长度：

```text
len=0.25
len=0.60
len=0.60
len=0.60
```

当前：

```yaml
min_path_length: 1.0
```

所以这些路径虽然有收益、有 clearance、方向也通过，但全部因为长度不够被过滤掉，导致 empty path。

另一个问题是：

```text
Newly modified path is not collision-free.
[RRG][MODPATH] reject segment ...
```

这说明 `improveFreePath()` 安全增强阶段有时把路径改得过激，修改后的路径反而没通过碰撞检查。不过这不是最终一定失败，因为日志中也有成功输出：

```text
[RRG][CLEARANCE] ... final path accepted: min=0.846 required=0.550
GBPlanner service response status=0 path_size=49
```

### 当前建议的下一轮调参

为了减少等待和 empty path，建议下一轮先只调三个参数：

```yaml
min_path_length: 0.45
safety_extension: [2.2, 2.2, 1.5]
edge_overshoot: 0.45
```

目的：

- `min_path_length: 0.45`：允许短距离局部脱困/小步前进，避免 `pass_length=0/N`。
- `safety_extension` 降低：减弱 `improveFreePath()` 推路径的力度，减少“改完反而碰撞”的概率。
- `edge_overshoot` 降低：减少边检查两端外扩过大导致的误拒绝。

保留不动：

```yaml
wall_clearance_enable: true
wall_clearance_min: 0.55
wall_clearance_reject_final_path: true
```

原因：防撞/远离墙体仍然是最高优先级，不建议为了减少 empty path 直接关掉 clearance 保护。

## 本次规划耗时统计

统计来源：`~/.ros/log/latest/rosout.log` 中的：

```text
GBPlanner service request seq=...
GBPlanner service response status=... path_size=...
```

这表示 PCI 调用 GBPlanner service 到 GBPlanner 返回的总耗时，包含 reset、建图、评价、最终路径生成等。

本次共统计到 48 次规划请求：

```text
总次数: 48
平均耗时: 0.204 s
中位数: 0.084 s
最快: 0.000 s
最慢: 1.456 s
```

成功返回非空路径的 12 次：

```text
平均耗时: 0.439 s
最慢: 1.163 s
```

返回 empty path 的 36 次：

```text
平均耗时: 0.125 s
最慢: 1.456 s
```

这说明当前“等很久”的体感，不完全是单次规划计算很慢，而是多次规划连续返回 empty path 后，PCI 不断重试，累积起来看起来像卡住。

### 单次成功规划的典型耗时

本次日志中几个成功样本：

```text
seq=2  bound=2 path_size=64 耗时 0.538 s
seq=7  bound=1 path_size=37 耗时 0.383 s
seq=12 bound=1 path_size=61 耗时 0.782 s
seq=21 bound=2 path_size=49 耗时 0.481 s
seq=45 bound=0 path_size=59 耗时 1.163 s
seq=47 bound=1 path_size=49 耗时 0.538 s
```

单次成功规划通常在 `0.38-0.78s`，偶尔到 `1.16s`。

### 已有分段计时口径

代码里已经有 `/gbp_time_log`，类型是 `std_msgs/Float32MultiArray`。含义在 `rrg.h` 中：

```text
data[0] = build_graph_time
data[1] = compute_exp_gain_time
data[2] = shortest_path_time
data[3] = evaluate_graph_time
```

这四段分别表示：

- `build_graph_time`：RRG 建图时间，包括采样、碰撞检查、连边。
- `compute_exp_gain_time`：计算探索收益/未知体素收益的时间。
- `shortest_path_time`：图上最短路/候选路径回溯相关时间。
- `evaluate_graph_time`：候选路径综合评价时间，包括 gain、长度、方向、history、clearance 等过滤和打分。

当前 `rosout.log` 里没有直接打印这四个数，所以要看分段耗时，需要运行时监听：

```bash
rostopic echo /gbp_time_log
```

建议下一轮测试时同时开一个终端记录：

```bash
rostopic echo /gbp_time_log > /tmp/gbp_time_log.txt
```

这样才能针对性判断到底慢在建图、gain 计算、最短路，还是路径评价。

### 目前最值得针对性提速的地方

根据本次日志，优先级如下：

1. 先减少 empty path 重试次数：调低 `min_path_length`，否则再快也会连续失败。
2. 减少 `improveFreePath()` 失败重试：适当降低 `safety_extension`，观察 `Newly modified path is not collision-free` 是否减少。
3. 如果 `/gbp_time_log` 显示 `build_graph_time` 高，再调 `num_vertices_max`、`num_edges_max`、`num_loops_cutoff`、`num_loops_max`。
4. 如果 `compute_exp_gain_time` 高，再考虑降低 gain 计算负担，例如减少候选叶子数量、降低 ray casting 分辨率或减少可视化。
5. 如果 `evaluate_graph_time` 高，再看新增的 clearance/centerline/smoothness 是否采样太密。

### 当前影响速度的主要参数

RRG 建图相关：

```yaml
num_vertices_max
num_edges_max
num_loops_cutoff
num_loops_max
nearest_range
nearest_range_max
edge_length_max
edge_overshoot
```

收益计算相关：

```yaml
use_ray_model_for_volumetric_gain
exp_gain_voxel_size
unknown_voxel_gain
free_voxel_gain
leafs_only_for_volumetric_gain
cluster_vertices_for_gain
clustering_radius
```

路径评价/安全相关：

```yaml
wall_clearance_sample_step
wall_clearance_penalty
wall_clearance_soft
path_safety_enhance_enable
safety_extension
path_interpolation_distance
min_path_length
```

下一步建议先不要盲目降 RRG 搜索规模，因为这次单次规划并不算特别慢，真正拖慢的是 empty path 和失败重试。

## 今天主要目标

今天主要围绕两个问题处理：

1. 无人机在 `floor5` 仿真里离墙太近，拐弯处容易切墙甚至撞墙。
2. 无人机有时会长时间停住，日志里常见碰撞检查失败、起点不安全、empty path 等现象。

后半段已经从单纯调参进入到算法层改造：给 GBPlanner 增加 ESDF clearance-aware 逻辑，让规划器不只判断“会不会撞”，还会尽量选离墙更远的路径。

## 今日重要备份

所有关键修改前都做了备份，主要备份目录如下：

- `config_backups/rmf_obelix_before_clear_sphere_1p5_20260709/`
- `config_backups/rmf_obelix_before_reduce_wait_empty_path_20260709/`
- `config_backups/rmf_obelix_before_target_wall_safety_rebalance_20260709/`
- `config_backups/rmf_obelix_before_wall_clearance_recovery_20260709/`
- `config_backups/rmf_obelix_before_clearance_aware_planning_20260709/`

最新算法层改造前的备份在：

```bash
config_backups/rmf_obelix_before_clearance_aware_planning_20260709/
```

里面备份了：

- `params.h`
- `params.cpp`
- `rrg.h`
- `rrg.cpp`
- `gbplanner_config.yaml`
- `README.md`

## 已做的调参方向

### 1. 增大安全边界

在 `rmf_obelix/gbplanner_config.yaml` 中调整过机器人碰撞盒和安全扩展参数，让规划更保守，尽量不要贴墙：

- `size_extension_min`
- `size_extension`
- `safety_extension`
- `edge_overshoot`
- `bound_mode`

当前思路是：不要一味把碰撞盒调得过大，否则容易导致起点附近被判不安全，出现 `Starting position is not clear`、`Planner returned an empty path`，所以后面又做过一轮回收和平衡。

### 2. 减少长时间停住

针对长时间停住和反复 empty path，调整过 RRG 搜索规模和短路径脱困相关参数：

- `num_vertices_max`
- `num_edges_max`
- `num_loops_cutoff`
- `num_loops_max`
- `min_path_length`
- `clear_sphere_radius`

判断结论：慢很多时候不是单次规划本身慢，而是卡在“起点或候选路径碰撞检查失败 -> 多轮 empty path -> 等下一次可行规划”。

### 3. 处理 TF/time 干扰

之前出现过 `TF_OLD_DATA`、`TF_REPEATED_DATA`、RViz 不显示等问题。判断主要和旧 ROS/Gazebo/PX4 进程残留、仿真时间跳变、重复 TF 发布有关。

建议明天每次正式测试前先清理旧进程，再启动仿真。当前已有脚本：

```bash
scripts/kill_current_sim.bash
scripts/run_current_sim.bash
```

## 今天新增的算法功能

### 功能名

ESDF clearance-aware planning，也就是“离墙距离感知规划”。

### 核心目的

以前规划器更像是：

```text
只要不撞，就可以作为候选路径
```

现在增加为：

```text
不撞只是底线；离墙太近的候选路径会被扣分，低于硬安全距离的路径会被拒绝
```

### 新增参数

位置：

```bash
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml
```

当前新增参数：

```yaml
wall_clearance_enable: true
wall_clearance_min: 0.55
wall_clearance_soft: 1.00
wall_clearance_penalty: 2.0
wall_clearance_sample_step: 0.30
wall_clearance_reject_final_path: true
```

参数含义：

- `wall_clearance_enable`：总开关，`false` 时回到旧逻辑。
- `wall_clearance_min`：硬安全距离，路径采样点低于这个距离会被拒绝。
- `wall_clearance_soft`：软安全距离，低于这个距离但高于 `min` 时会扣分。
- `wall_clearance_penalty`：贴墙惩罚强度，越大越不愿意选贴墙路径。
- `wall_clearance_sample_step`：沿路径采样检查 ESDF 距离的间隔。
- `wall_clearance_reject_final_path`：最终输出前再检查一次，太贴墙就返回 empty path，避免执行危险路径。

### 修改的代码文件

- `src/exploration/gbplanner_ros/planner_common/include/planner_common/params.h`
- `src/exploration/gbplanner_ros/planner_common/src/params.cpp`
- `src/exploration/gbplanner_ros/gbplanner/include/gbplanner/rrg.h`
- `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp`
- `src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml`

### 关键实现点

1. `PlanningParams` 增加了 6 个 `wall_clearance_*` 参数。
2. `Rrg::evaluateGraph()` 中对每条候选路径计算 ESDF clearance cost。
3. 候选路径增益被修改为：

```text
path_gain *= exp(-wall_clearance_penalty * clearance_cost)
```

4. 如果候选路径最小 ESDF 距离低于 `wall_clearance_min`，不参与最佳路径竞争。
5. `Rrg::getBestPath()` 和 `Rrg::getBestPathSimplified()` 最终输出路径前增加二次 clearance 检查。
6. 新增日志：

```text
[RRG][CLEARANCE] leaf=... min=... cost=... pass=... gain=...
```

## 已验证

### YAML 检查

已执行：

```bash
python3 - <<'PY'
import yaml
p='src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml'
yaml.safe_load(open(p))
print('YAML OK:', p)
PY
```

结果：通过。

### 编译检查

已执行：

```bash
catkin build planner_common gbplanner
```

结果：

```text
All 24 packages succeeded
No packages failed
```

编译有 warning，但来自已有依赖和旧代码，例如 `adaptive_obb`、`voxblox_ros`，不是这次新增功能导致的编译失败。

## 明天测试建议

### 1. 先清理旧进程

正式测试前建议先清理旧仿真进程，避免 TF/time 干扰：

```bash
cd /home/uestc/gb3/uestc-gbplanner
bash scripts/kill_current_sim.bash
```

### 2. 重新启动仿真

注意：今天编译通过后，旧的 `gbplanner_node` 不会自动变成新代码，必须重启仿真或至少重启 `gbplanner_node`。

常用启动：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch gbplanner rmf_sim.launch rviz_en:=true traj_server_en:=true
```

控制：

```bash
cd /home/uestc/gb3/uestc-gbplanner
export ROS_MASTER_URI=http://localhost:11311
unset ROS_IP
export ROS_HOSTNAME=localhost
export PX4_FIRMWARE_ROOT=/home/uestc/PX4-Autopilot
unset PX4_BUILD_DIR
source scripts/gb3_env.bash
roslaunch px4ctrl run_ctrl.launch
```

### 3. 重点看这些日志

```text
[RRG][CLEARANCE]
Starting position is not clear
Current box contains Occupied voxels
Planner returned an empty path
Newly modified path is not collision-free
GBPlanner service response status=0 path_size=...
```

重点观察：

- `[RRG][CLEARANCE]` 里的 `min` 是否大多数高于 `0.55`。
- `pass=0` 是否特别多。
- `Planner returned an empty path` 是否明显增加。
- 粉色执行路径是否还贴墙或穿墙。
- 无人机转弯处是否仍然切墙。

## 明天优先调参顺序

### 情况 A：离墙明显改善，但 empty path 变多

优先把硬安全距离稍微降低：

```yaml
wall_clearance_min: 0.45
```

先不要直接关闭 `wall_clearance_enable`。

### 情况 B：还是贴墙

可以加大软惩罚，不一定先加硬阈值：

```yaml
wall_clearance_soft: 1.20
wall_clearance_penalty: 3.0
```

这样会更偏向离墙远的候选路径，但不会像提高 `wall_clearance_min` 那样直接拒绝太多路径。

### 情况 C：规划太慢

先观察是否因为 `[RRG][CLEARANCE] pass=0` 太多导致可行路径少。如果是，可先降低：

```yaml
wall_clearance_min: 0.45
```

如果不是 clearance 导致，再回到 RRG 搜索规模参数：

- `num_vertices_max`
- `num_edges_max`
- `num_loops_cutoff`
- `num_loops_max`

### 情况 D：想做对照实验

直接关掉新功能：

```yaml
wall_clearance_enable: false
```

然后重启 `gbplanner_node`，行为应基本回到旧逻辑。

## 当前判断

单纯调大碰撞盒会让无人机更保守，但也容易把起点和窄处判成不可行，导致停住。今天新增的 clearance-aware planning 更像是在“路径选择层”远离墙体，不完全依赖把碰撞盒调大，因此更适合后续继续精调。

明天建议先不要同时大改很多参数。优先围绕 `wall_clearance_min`、`wall_clearance_soft`、`wall_clearance_penalty` 做小步对照。
