# 影响规划速度的参数

本文记录本工作空间 `gbplanner3` 实机配置中，已经调整过、且会明显影响地图更新和规划速度的参数。

当前启动入口：

```bash
roslaunch gbplanner rmf_real.launch
```

该 launch 会加载：

- `src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/voxblox_sim_config.yaml`
- `src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/planner_control_interface_real_config.yaml`

## 1. voxblox 地图处理与发布

文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/voxblox_sim_config.yaml
```

### 当前参数

```yaml
max_ray_length_m: 15.0
update_mesh_every_n_sec: 2.0
publish_pointclouds: True
pointcloud_queue_size: 5
min_time_between_msgs_sec: 0.10
publish_traversable: False
publish_tsdf_map: False
publish_esdf_map: False
```

### 为什么这些参数会影响速度

`pointcloud_queue_size`

原来是 `1000`。如果 gbplanner_node 处理点云跟不上输入频率，ROS 会排很多旧点云，地图就会慢慢处理历史数据，看起来像“地图延迟更新”。现在改成 `5`，让系统丢掉过旧数据，优先处理新数据。

`min_time_between_msgs_sec`

原来是 `0.0`，表示每一帧点云都尝试进入 voxblox。FASTLIVO 的 `/cloud_registered_fixed` 大约 10 Hz 左右，单帧约 0.54 MB，全部积分会让 gbplanner_node 长时间高 CPU。现在改成 `0.10`，约等于最多 10 Hz 处理。

`publish_pointclouds`

保持为 `True`，因为调试时需要看 `/gbplanner_node/surface_pointcloud` 和 `/gbplanner_node/tsdf_pointcloud`。如果规划仍然很卡，可以临时改成 `False` 做纯规划性能测试。

`update_mesh_every_n_sec`

控制 voxblox mesh/地图点云的刷新周期。原来 `1.0` 会每秒发布一次较重的全图可视化；调试时如果 `5.0` 太慢，可以改成 `2.0`。当前使用 `2.0`，目标是让地图点云约 2 秒刷新一次，同时避免恢复到原始配置那种高负载状态。

`publish_tsdf_map` / `publish_esdf_map`

这两个不是 RViz 点云显示必需项，而是发布完整 map layer，数据量和序列化开销较大。当前关闭，避免无订阅需求时也增加负担。

`max_ray_length_m`

原来是 `50.0`。实机规划传感器配置里 OS064 的 `max_range` 是 15m，voxblox 再按 50m 射线积分没有必要，会增加清空/射线遍历成本。现在改成 `15.0`。

## 2. PCI 路径执行与重规划触发

文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/planner_control_interface_real_config.yaml
```

### 当前参数

```yaml
path_end_dist_thr: 1.0
path_progression_dist_thr: 1.05
path_progression_dist_thr_min: 0.85
stuck_timeout: 2.0
stuck_dist_margin: 0.03
```

### 为什么这些参数会影响路径更新

PCI 只有在 `pci_status == kReady` 时才会再次调用 planner。执行当前路径时，如果卡在某个 waypoint 附近但又没有满足通过阈值，就不会进入 Ready，也不会触发下一次规划。

之前日志里出现过：

```text
[PCI][EXEC] wp=20/138 dist=0.85 thr=0.82
[PCI][RUN] executing status=RUNNING trigger=AUTO
```

这表示距离 waypoint 还有 0.85m，但通过阈值是 0.82m，差 0.03m 没通过；同时原来的 stuck 条件又太宽，不会释放为 Ready。因此路径更新看起来很慢，甚至像卡住。

当前调整：

- 放宽 waypoint 通过距离，避免边界抖动卡住。
- 缩短 stuck 检测时间，让执行器更快释放到 Ready。
- 减小 stuck 距离余量，让“接近但过不去”的状态更快触发重规划。

## 3. 调试建议

重启 gbplanner 后检查参数：

```bash
rosparam get /gbplanner_node/pointcloud_queue_size
rosparam get /gbplanner_node/min_time_between_msgs_sec
rosparam get /gbplanner_node/publish_pointclouds
rosparam get /gbplanner_node/update_mesh_every_n_sec
rosparam get /gbplanner_node/publish_tsdf_map
rosparam get /gbplanner_node/publish_esdf_map

rosparam get /pci_general_ros_node/path_end_dist_thr
rosparam get /pci_general_ros_node/path_progression_dist_thr
rosparam get /pci_general_ros_node/path_progression_dist_thr_min
rosparam get /pci_general_ros_node/stuck_timeout
rosparam get /pci_general_ros_node/stuck_dist_margin
```

查看输入点云是否正常：

```bash
rostopic hz /cloud_registered_360
rostopic hz /cloud_registered_fixed
rostopic delay /cloud_registered_fixed
```

查看 voxblox 地图点云：

```bash
rostopic hz /gbplanner_node/surface_pointcloud
rostopic hz /gbplanner_node/tsdf_pointcloud
```

当前 `update_mesh_every_n_sec: 2.0`，所以地图点云大约每 2 秒刷新一次是正常的。如果实际频率明显低于 0.5 Hz，说明 `gbplanner_node` 已经在地图生成或规划计算上吃紧。

查看是否卡在 waypoint：

```bash
rostopic echo /rosout | grep -E "\\[PCI\\]\\[EXEC\\]|\\[PCI\\]\\[RUN\\]"
```

如果反复看到类似下面的日志，说明 PCI 还在执行旧路径，没有释放到下一轮规划：

```text
[PCI][EXEC] wp=... dist=... thr=...
[PCI][RUN] executing status=RUNNING trigger=AUTO
```

## 4. 两种推荐模式

### 调试地图模式

适合需要在 RViz 里看 voxblox 点云地图：

```yaml
publish_pointclouds: True
update_mesh_every_n_sec: 5.0
pointcloud_queue_size: 5
min_time_between_msgs_sec: 0.10
publish_tsdf_map: False
publish_esdf_map: False
```

如果 `5.0` 看图太慢，可以使用当前更积极的调试值：

```yaml
publish_pointclouds: True
update_mesh_every_n_sec: 2.0
pointcloud_queue_size: 5
min_time_between_msgs_sec: 0.10
publish_tsdf_map: False
publish_esdf_map: False
```

### 纯规划性能模式

适合排查规划本身是否变快：

```yaml
publish_pointclouds: False
update_mesh_every_n_sec: 0.0
pointcloud_queue_size: 5
min_time_between_msgs_sec: 0.10
publish_tsdf_map: False
publish_esdf_map: False
```

切换参数后需要重启：

```bash
roslaunch gbplanner rmf_real.launch
```
