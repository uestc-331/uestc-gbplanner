主要代码位置
1. 核心实现代码 - 历史惩罚计算逻辑
文件: src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
第1682-1728行: 在 evaluateGraph() 函数中计算历史惩罚
计算当前路径方向与历史路径方向的相似度
应用时间衰减权重
使用 path_history_penalty 系数对路径增益施加惩罚
rrg.cppLines 1682-1728
      // ========== History Path Penalty (Anti-Oscillation Mechanism) ==========      // Apply penalty for recently attempted path directions to prevent oscillation      double history_penalty = 0.0;      if (!recent_path_directions_.empty() && path_list.size() >= 2) {        // Compute current path direction (from root to leaf)        Eigen::Vector3d current_path_direction =            (path_list[0] - path_list.back()).normalized();        double current_time = ros::Time::now().toSec();        double total_weight = 0.0;        // Compare with each historical path direction        for (size_t h = 0; h < recent_path_directions_.size(); ++h) {          double time_diff = current_time - recent_path_timestamps_[h];          double time_weight = exp(-time_diff / kHistoryDecayTime);  // Exponential decay          // Compute direction similarity (dot product)          // FIXED: Penalize paths that DIFFER from historical direction          // similarity = 1 means same direction (no penalty)          // similarity = 0 means perpendicular (medium penalty)          // similarity = -1 means opposite direction (maximum penalty)          double direction_similarity =              current_path_direction.dot(recent_path_directions_[h]);          // Convert similarity to dissimilarity: dissimilar paths get penalized          // dissimilarity ranges from 0 (same direction) to 2 (opposite direction)          double dissimilarity = 1.0 - direction_similarity;  // 0 to 2          dissimilarity = std::max(0.0, dissimilarity);  // Ensure non-negative          history_penalty += dissimilarity * time_weight;          total_weight += time_weight;        }        // Normalize by total weight        if (total_weight > 0.0) {          history_penalty /= total_weight;        }        // Apply history penalty to path gain        double lambda_history = planning_params_.path_history_penalty;        double path_gain_before_history = path_gain;        path_gain *= exp(-lambda_history * history_penalty);        ROS_DEBUG("[RRG] Path %d: history_penalty=%.3f, gain: %.2f -> %.2f",                  id, history_penalty, path_gain_before_history, path_gain);      }      // ========== End of History Path Penalty ==========
第1824-1844行: 记录选中路径方向到历史队列
rrg.cppLines 1824-1844
    // ========== Record Selected Path Direction to History ==========    // Store the chosen path direction to prevent oscillation in future decisions    std::vector<Eigen::Vector3d> chosen_path_list;    local_graph_->getShortestPath(best_path_id, local_graph_rep_, true, chosen_path_list);    if (chosen_path_list.size() >= 2) {      Eigen::Vector3d chosen_direction =          (chosen_path_list[0] - chosen_path_list.back()).normalized();      recent_path_directions_.push_back(chosen_direction);      recent_path_timestamps_.push_back(ros::Time::now().toSec());      // Limit history size      if (recent_path_directions_.size() > kMaxHistorySize) {        recent_path_directions_.pop_front();        recent_path_timestamps_.pop_front();      }      ROS_INFO("[RRG] Path direction added to history: [%.2f, %.2f, %.2f], history_size=%zu",               chosen_direction.x(), chosen_direction.y(), chosen_direction.z(),               recent_path_directions_.size());
2. 参数定义
文件: src/exploration/gbplanner_ros/planner_common/include/planner_common/params.h
第303行: path_history_penalty 参数声明
params.hLines 303-305
  double path_history_penalty;  int path_history_size;  double path_history_decay_time;
3. 参数加载
文件: src/exploration/gbplanner_ros/planner_common/src/params.cpp
第961-965行: 从 ROS 参数服务器加载 path_history_penalty 参数
params.cppLines 961-965
  param_name = ns + "/path_history_penalty";  if (!ros::param::get(param_name, path_history_penalty)) {    path_history_penalty = 1.0;  // default history penalty coefficient    ROSPARAM_WARN(param_name, path_history_penalty);  }
4. 历史记录数据结构
文件: src/exploration/gbplanner_ros/gbplanner/include/gbplanner/rrg.h
第488-491行: 历史路径方向和时间戳的存储结构
rrg.hLines 488-491
  std::deque<Eigen::Vector3d> recent_path_directions_;  // Recent N path directions  std::deque<double> recent_path_timestamps_;           // Corresponding timestamps  static constexpr int kMaxHistorySize = 8;             // INCREASED: Max history size (was 5, now 8)  static constexpr double kHistoryDecayTime = 45.0;     // INCREASED: History decay time in seconds (was 30.0, now 45.0)
5. 配置文件
src/exploration/gbplanner_ros/gbplanner/config/smb/gbplanner_config.yaml (第136行)
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml (第144行)
这些是历史惩罚系数相关的主要代码位置。核心逻辑在 rrg.cpp 的 evaluateGraph() 函数中。
