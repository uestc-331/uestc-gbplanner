# 算法逻辑学习

## 1. 回家状态怎么看

手动触发回家命令：

```bash
rostopic pub -1 /gbplanner_ui/cmd/homing std_msgs/Empty "{}"
```

这个话题只是 UI 命令入口，不是回家状态本身。它会被 `gbplanner_topic_command_bridge` 转成服务调用：

```text
/gbplanner_ui/cmd/homing
  -> /planner_control_interface/std_srvs/homing_trigger
  -> planner_control_interface::runHoming()
  -> gbplanner/homing
  -> PCI 执行 homing path
```

真正用于观察“当前是否正在执行回家路径”的话题是：

```bash
rostopic echo /gbplanner_is_homing
```

含义：

```text
true  = 当前执行的是 homing path / 回家路径
false = 当前不是回家路径
```

相关状态话题：

```bash
rostopic echo /gbplanner_status
```

`/gbplanner_status` 是规划是否成功的 `std_msgs/Bool`，不是“正在回家”的状态。

## 2. 当前配置中的时间返航参数

配置文件：

```text
src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml
```

关键参数：

```yaml
PlanningParams:
  v_max:            0.1
  v_homing_max:     0.1
  time_budget_limit: 3600.0
  auto_homing_enable: true
  go_home_if_fully_explored: true
```

含义：

```text
v_homing_max        回家路径时间估计使用的速度，单位 m/s
time_budget_limit   总任务时间预算，单位 s
auto_homing_enable  是否启用时间预算自动返航
go_home_if_fully_explored 局部/全局探索完成后是否回家
```

注意：这里的 `v_homing_max` 属于 gbplanner 的规划时间估计参数，和 PCI 执行器里的轨迹速度参数不是同一个配置文件里的东西。

## 3. 时间返航核心公式

自动返航不是简单等到 `time_budget_limit` 到 0 才触发，而是提前判断“剩余时间是否还够安全回家”。

核心计算：

```text
time_elapsed = ros::Time::now() - rostime_start_

time_budget_remaining = time_budget_limit - time_elapsed

time_remaining = min(time_budget_remaining, current_battery_time_remaining_)

homing_len = length(homing_path)

time_to_home = homing_len / v_homing_max
```

触发自动返航条件：

```text
time_to_home > time_remaining - 20
```

等价理解：

```text
time_remaining < time_to_home + 20
```

其中 `20` 是代码里写死的安全余量：

```cpp
const double kTimeDelta = 20;
```

也就是说，系统会至少给回家预留：

```text
回家路径估计时间 + 20 秒安全余量
```

## 4. 举例

当前配置：

```text
v_homing_max = 0.1 m/s
time_budget_limit = 3600 s
```

如果当前到家的路径长度为：

```text
homing_len = 10 m
```

则：

```text
time_to_home = 10 / 0.1 = 100 s
触发阈值 = 100 + 20 = 120 s
```

当剩余时间小于约 `120 s` 时，系统就会触发自动返航。

如果当前到家的路径长度是：

```text
homing_len = 30 m
```

则：

```text
time_to_home = 30 / 0.1 = 300 s
触发阈值 = 300 + 20 = 320 s
```

当剩余时间小于约 `320 s` 时，就会触发自动返航。

所以返航触发时间会随“当前位置到家的路径长度”动态变化。

## 5. 整个时间返航逻辑流程

### 5.1 任务计时起点

RRG 初始化时记录任务开始时间：

```cpp
rostime_start_ = ros::Time::now();
```

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
```

如果调用重置计时服务，会重新设置任务起点：

```bash
rosservice call /gbplanner/reset_timer "{}"
```

对应函数：

```cpp
bool Rrg::resetTimerCallback(...)
```

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
```

### 5.2 行为树检查是否需要返航

行为树节点 `HomingCheck` 会调用：

```cpp
gbplanner_->homingRequired()
```

如果返回 `true`，行为树进入回家分支。

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/gbplanner_bt_nodes.cpp
```

关键函数：

```cpp
BT::NodeStatus HomingCheck::tick()
```

### 5.3 Gbplanner 转发到 RRG 判断

`Gbplanner::homingRequired()` 只是设置状态并转给 RRG：

```cpp
out_srv_res_.status = planner_msgs::planner_srv::Response::kHoming;
return rrg_->homingRequired(out_srv_res_.path);
```

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/gbplanner.cpp
```

关键函数：

```cpp
bool Gbplanner::homingRequired()
```

### 5.4 RRG 计算剩余时间和回家时间

核心判断函数：

```cpp
bool Rrg::homingRequired(std::vector<geometry_msgs::Pose>& homing_path)
```

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
```

主要逻辑：

```cpp
time_elapsed = ros::Time::now() - rostime_start_;
time_budget_remaining = planning_params_.time_budget_limit - time_elapsed;
time_remaining = std::min(time_budget_remaining, current_battery_time_remaining_);

homing_path = searchHomingPath(world_frame_, current_state_);
homing_len = Trajectory::getPathLength(homing_path);
time_to_home = homing_len / planning_params_.v_homing_max;

if (time_to_home > time_remaining - 20) {
  homing_engaged_ = true;
  return true;
}
```

### 5.5 正常规划时也会检查是否应该回家

在 `Rrg::getBestPath(...)` 中，如果启用了：

```yaml
auto_homing_enable: true
```

会在选取探索路径前检查是否应该直接返回 homing path。

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
```

关键函数：

```cpp
std::vector<geometry_msgs::Pose> Rrg::getBestPath(std::string tgt_frame, int& status)
```

触发后会设置：

```cpp
status = planner_msgs::planner_srv::Response::kHoming;
homing_engaged_ = true;
return homing_path;
```

### 5.6 全局目标选择也考虑返航时间

全局 frontier / global planner 选择目标时，会估计：

```text
time_to_target = current_to_frontier_distance / v_homing_max
time_to_home   = frontier_to_home_distance / v_homing_max
time_cost      = time_to_target + time_to_home
```

当 `auto_homing_enable: true` 时，目标必须满足：

```text
getTimeRemained() - time_cost >= 20
```

否则认为这个目标时间上不可行。

相关函数：

```cpp
double Rrg::getTimeRemained()
bool Rrg::isRemainingTimeSufficient(const double& time_cost, double& time_spare)
```

代码位置：

```text
src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp
```

## 6. 电池剩余时间的参与方式

RRG 里还有一个：

```cpp
current_battery_time_remaining_
```

默认值是无限大：

```cpp
current_battery_time_remaining_ = std::numeric_limits<double>::max();
```

如果 `/robot_status` 发布了 `time_remaining`，则会更新它：

```cpp
void Gbplanner::robotStatusCallback(const planner_msgs::RobotStatus& status) {
  rrg_->setTimeRemaining(status.time_remaining);
}
```

最终剩余时间取二者较小值：

```text
time_remaining = min(time_budget_limit - time_elapsed,
                     current_battery_time_remaining)
```

也就是说：

```text
时间预算先到 -> 触发返航
电池剩余时间先不足 -> 也触发返航
```

如果没有有效 `/robot_status`，通常只由 `time_budget_limit` 控制。

## 7. 代码位置总表

| 作用 | 文件 | 关键函数/位置 |
|---|---|---|
| 配置时间预算、返航速度 | `src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml` | `v_homing_max`, `time_budget_limit`, `auto_homing_enable` |
| 加载参数 | `src/exploration/gbplanner_ros/planner_common/src/params.cpp` | `auto_homing_enable`, `time_budget_limit`, `time_budget_before_landing` |
| 行为树判断返航 | `src/exploration/gbplanner_ros/gbplanner/src/gbplanner_bt_nodes.cpp` | `HomingCheck::tick()` |
| Gbplanner 转发返航判断 | `src/exploration/gbplanner_ros/gbplanner/src/gbplanner.cpp` | `Gbplanner::homingRequired()` |
| 核心时间返航判断 | `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp` | `Rrg::homingRequired(...)` |
| 正常探索路径选择中的返航判断 | `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp` | `Rrg::getBestPath(...)` |
| 剩余时间计算 | `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp` | `Rrg::getTimeElapsed()`, `Rrg::getTimeRemained()` |
| 时间可行性判断 | `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp` | `Rrg::isRemainingTimeSufficient(...)` |
| 重置任务计时 | `src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp` | `Rrg::resetTimerCallback(...)`, `/gbplanner/reset_timer` |
| UI homing 话题桥接 | `src/exploration/gbplanner_ros/gbplanner_ui/src/gbplanner_topic_command_bridge.cpp` | `/gbplanner_ui/cmd/homing` -> `/planner_control_interface/std_srvs/homing_trigger` |
| 执行回家路径 | `src/exploration/gbplanner_ros/planner_control_interface/src/planner_control_interface.cpp` | `runHoming(...)` |
| 发布是否正在回家 | `src/exploration/pci_general/src/pci_general.cpp` | `/gbplanner_is_homing` |

## 8. 调试时建议看的输出

查看是否正在执行回家路径：

```bash
rostopic echo /gbplanner_is_homing
```

查看规划成功状态：

```bash
rostopic echo /gbplanner_status
```

查看当前执行/发布的路径：

```bash
rostopic echo /gbplanner_path
```

查看日志中的时间判断：

```text
Time to home: ...
Time remaining: ...
REACHED TIME LIMIT: HOMING ENGAGED.
REACHED TIME LIMIT: BE CAREFUL.
```

重置任务计时：

```bash
rosservice call /gbplanner/reset_timer "{}"
```

