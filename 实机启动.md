# 目前问题
/home/super/uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/voxblox_sim_config.yaml
看这个


# 实机启动命令

当前工作空间路径：

```bash
/home/super/uestcgbplanner3
```

注意：旧文档中的 `rmf_real_robot.launch` 在当前源码树中不存在。当前可用的实机启动文件是：

```bash
rmf_real.launch
```

## 1. 启动规划器实机模式

新开一个终端执行：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch
```

## 2. 启动命令行 UI

再开一个终端执行：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
rosrun gbplanner_ui gbplanner_simple_topic_ui
```

## 3. 启动时打开 RViz

如果需要随规划器一起打开 RViz：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch rviz_en:=true
```

## 4. 默认实机话题

`rmf_real.launch` 默认使用以下话题：

```text
里程计输入: /vins_fusion/imu_propagate
点云输入:   /cloud_registered_360
轨迹输出:   /rmf_obelix/command/trajectory
控制桥接:   /position_cmd
```

## 5. 自定义里程计和点云话题

如果实机上的里程计或点云话题不同，启动时覆盖参数：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch odometry_topic:=/你的里程计话题 planner_pointcloud_topic:=/你的点云话题
```

示例：

```bash
roslaunch gbplanner rmf_real.launch odometry_topic:=/vins_fusion/imu_propagate planner_pointcloud_topic:=/cloud_registered_360
```

## 6. 一键启动规划器并进入 UI

如果只需要启动规划器并随后进入命令行 UI，可以使用：

```bash
cd /home/super/uestcgbplanner3
source /opt/ros/noetic/setup.bash
source /home/super/uestcgbplanner3/devel/setup.bash
roslaunch gbplanner rmf_real.launch & sleep 2
rosrun gbplanner_ui gbplanner_cli
wait
```
这套配置里 planner_control_interface 的轨迹速度是 1m/s，但 gbplanner_config.yaml 里的规划速度只有 v_max=0.1、v_homing_max=0.2；这会让预算/全局选择偏保守，不一定让 CPU
  慢，但会让“规划进展”看起来慢。我再找实际 v_max/global_graph_odom 是否缺省，给你一个可执行的排查结论。我已经能给出初步结论：不是 bag 话题名不匹配导致的慢，而是当前规划
  参数和运行方式会让每次规划搜索量偏大，并且回放时可能额外放大全局图维护开销。我再取一下精确行号，方便你按文件定位。

• Explored
  └ Read gbplanner_config.yaml, voxblox_sim_config.yaml, rrg.cpp

─ Worked for 1m 35s ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我查了一圈，当前路径规划慢主要不像是 bag 话题不匹配，而是规划器一次规划的搜索/地图处理量偏大。

  主要慢点

  1. 局部采样空间太大
     uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml:73 里 Local 是 [-20,-20,-3] 到 [20,20,3]，也就是 40m x 40m x 6m。对
     管道/机关雷达回放来说范围偏大，会导致很多采样点和碰撞检测浪费。
  2. 单次建图上限偏高
     uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml:182：
     num_vertices_max=500、num_edges_max=7000、num_loops_max=3000。
     实际建图循环在 uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/src/rrg.cpp:2050，每次循环都会采样、查邻居、做边碰撞检测，慢的时候通常卡在这里。
  3. 地图积分和可视化发布负担重
     uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/voxblox_sim_config.yaml:5 里 TSDF 分辨率是 0.20m，max_ray_length_m=50.0，并且：
      - publish_pointclouds: True
      - publish_traversable: True
      - publish_tsdf_map: True
      - publish_esdf_map: True

     这些在回放 bag 时会占 CPU，影响规划线程响应。
  4. 每次规划还会算体素增益
     uestcgbplanner3/src/exploration/gbplanner_ros/gbplanner/config/rmf_obelix/gbplanner_config.yaml:190 使用 ray model 计算增益，传感器 OS064 最大范围 15m。叶子节点多
     的时候，增益计算也会明显变慢。

  建议先试的参数

  把本次 bag 回放调试用参数先降下来：

  Local:
    min_val: [-8.0, -8.0, -2.0]
    max_val: [8.0, 8.0, 2.0]

  num_vertices_max: 200
  num_edges_max: 2000
  num_loops_cutoff: 600
  num_loops_max: 1000

  地图发布调试时先关掉：

  publish_pointclouds: False
  publish_traversable: False
  publish_tsdf_map: False
  publish_esdf_map: False

  现场确认慢点的日志

  运行时重点看这些日志：

  rosconsole | grep -E "Formed a graph|Chrono|runGlobalPlanner costs|RRG\\]\\[DIAG\\]"

  如果看到 Formed a graph ... loops 经常接近 3000，就是采样建图慢。
  如果 Chrono 里 compute_exp_gain_time 或 evaluate_graph_time 大，就是增益/路径评估慢。
  如果 CPU 很高但规划日志间隔长，优先关 voxblox 的 map/pointcloud 发布。

  # 调试录包
  1./gbplanner_is_homing 是否触发回家
  2./vis/ref_path 规划的路径
  3./gbplanner_node/surface_pointcloud 可视化点云 
  4./vins_fusion/imu_propagate  # 定位相关

## 相关指令 关键版本 
rosbag record --lz4 \
  -O ~/gbplanner_bags/gbplanner_key_debug \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate

分包版：rosbag record --lz4 --split --duration=5m \
  -O ~/gbplanner_bags/gbplanner_key_debug \
  /gbplanner_is_homing \
  /vis/ref_path \
  /gbplanner_node/surface_pointcloud \
  /vins_fusion/imu_propagate


# Frontier / 全局图 / 全局路径
/vis/planning_global_graph     # global graph + frontier 点，marker ns 里有 frontier/frontiers
/vis/planning_global_path      # current->frontier 和 frontier->home 两段路径
/vis/planning_graph            # 本地 RRG 图，里面也有局部 frontier 信息
/vis/best_planning_paths       # 本地候选/最佳规划路径
/vis/shortest_paths            # 最短路径候选，可辅助复盘 frontier 选择

# 回家路径
/vis/planning_homing_path      # 专门的 homing path 可视化
/vis/ref_path                  # 当前规划参考路径，homing 时也会显示回家参考路径
/vis/imp_ref_path              # 改进/避障后的参考路径，如果有发布
/gbplanner_path                # PCI 实际交给执行层的 nav_msgs/Path

# 规划给控制端要执行的路径/轨迹
/rmf_obelix/command/trajectory # gbplanner/PCI 输出的 MultiDOFJointTrajectory
/pci_command_trajectory_vis    # PCI 执行轨迹可视化
/position_cmd                  # 轨迹桥接后给控制器的最终位置命令


## 详细版本录包指令
mkdir -p ~/gbplanner_bags

rosbag record /gbplanner_is_homing   /vis/ref_path   /gbplanner_node/surface_pointcloud   /vins_fusion/imu_propagate   /vis/planning_global_graph   /vis/planning_global_path   /vis/planning_graph   /vis/best_planning_paths   /vis/shortest_paths   /vis/planning_homing_path   /vis/imp_ref_path   /gbplanner_path   /rmf_obelix/command/trajectory   /pci_command_trajectory_vis   /position_cmd    -O 20260515

